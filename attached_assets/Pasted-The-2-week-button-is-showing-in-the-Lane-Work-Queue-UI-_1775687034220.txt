The 2+/week button is showing in the Lane Work Queue UI, but the page is still returning zero lanes.

Important evidence from the screenshot:
- scope says “Showing: All org lanes”
- all four bucket counts are 0
- admin debug says “0 lanes across 0 buckets”
This suggests the backend result for this page is empty, not just visually filtered out on the client.

I need you to debug the exact path for the 2+/week filter and tell me why no lanes are appearing.

Please inspect this in order:

1) Verify whether recurring lanes currently exist in the database for my org
Return:
- total recurring lanes
- total active recurring lanes
- how many have loads/week >= 2
- how many have loads/week < 2

2) Verify the work queue API response without the 2+/week filter
Tell me:
- how many lanes are returned total
- how many are in each bucket
- whether the API is already returning zero before the filter is applied

3) Verify the 2+/week filter logic
- Is the filter being applied client-side or server-side?
- What exact field is it using? (loadsPerWeek, weeklyLoadCount, averageLoadsPerWeek, etc.)
- Is that field actually populated on the lane objects returned to the page?
- Is the comparison numeric or string-based?
- Are there null/undefined/default-zero issues causing all lanes to fail the filter?

4) Verify scope / hierarchy filtering
- Since the page says “All org lanes,” confirm whether this user truly has org-wide visibility
- Confirm whether the same lanes visible in DB are within this user’s visibility scope
- Identify if the queue query is filtering them out due to owner/team/hierarchy conditions

5) Verify eligibility logic
- If recurring lanes exist, explain why they are not considered “eligible lanes needing attention”
- Check whether they are being excluded because of:
  - resolved/snoozed state
  - missing assignment state
  - completion threshold logic
  - no bench rows
  - wrong bucket logic
  - active/inactive filtering

6) Fix only the root issue
- If the lanes exist and should appear, fix the bug
- If the 2+/week filter is wrong, fix the filter
- If the page is accidentally querying an empty scope, fix that
- If the generated lanes are below 2/week, say that clearly

Output format:
1. CURRENT DB STATE
2. CURRENT API RESPONSE
3. ROOT CAUSE
4. FIXES MADE
5. WHAT I SHOULD SEE IN THE QUEUE AFTER THE FIX