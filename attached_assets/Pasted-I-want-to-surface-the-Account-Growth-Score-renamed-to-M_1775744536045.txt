I want to surface the Account Growth Score (renamed to “Momentum Score” in the UI) directly on the customer contact card in the Customer tab.

Goal
Make the score visible where AMs actually work the account, not just in backend or reports. When I open a customer, I should immediaible where AMs actually work the account, not just in backend or reption.

Current state
- server/growthScoreCalculator.ts computes a 0–100 Account Growth Score per account, with bands:
  - 76–100: high_expansion (Primed to Grow)
  - 51–75: growth_ready (Growth Ready)
  - 26–50: stable (Stable)
  - 0–25: at_risk (At Risk)
- nextBestActionEngine.ts uses the score for NBA rules.
- The score object (GrowthScoreResult / GrowthScoreDriver) is already available server-side.
- The Customer tab has a customer contact card/component but does NOT currently display this score.

What to implement

1) Naming / copy
- Backend: keep type names as Account Growth Score to avoid refactor churn.
- Frontend UI label: “Momentum Score”.
- Band labels:
  - Primed to Grow
  - Growth Ready
  - Stable
  - At Risk

2) Data plumbing
- Expose the latest Momentum Score and band for each account on the customer detail API that feeds the Customer tab.
  - If there is an existing customer detail endpoint, extend its response to include:
    - momentumScore: number (0–100)
    - momentumBand: 'high_expansion' | 'growth_ready' | 'stable' | 'at_risk'
    - updatedAt: ISO string
- If scores are already stored and refreshed elsewhere, reuse that; otherwise:
  - either compute on demand when fetching the customer,
  - or read from the existing stored score location (whichever is how the app currently works).
- Do NOT change the underlying scoring logic in this task.

3) UI placement
- On the customer contact card in the Customer tab, add a small Momentum section.
- Display:
  - numeric score (e.g., “82”)
  - band label (e.g., “Primed to Grow”)
  - chip or pill with color based on band:
    - Primed to Grow: green
    - Growth Ready: blue
    - Stable: amber
    - At Risk: red
- Design should be compact, e.g.:
  - “Momentum Score: 82 · Primed to Grow” with a colored chip,
  - placed near the top of the card with other key stats (name, status, etc.).

4) Hover / tooltip explanation
- On hover over the score or band, show a short explanation:
  - “Momentum Score is a 0–100 signal based on touchpoint health, relationship depth, volume, lane breadth, RFPs, and behaible where AMs actually work the account, not just in backend or rep to it if easy (optional, not required for this task).

5) Empty / fallback states
- If a score is not yet available for this customer:
  - Show “Momentum Score: —” and a subtle “Not yet calculated” tooltip.
  - Do NOT block the page or show errors.
- Once the score is available, the UI should update on next load without any special action.

6) Performance / calls
- Avoid adding an extra network call just for the score:
  - Prefer extending the existing customer detail response.
- If a separate call is absolutely necessary, cache results client-side for the current session to avoid repeated fetches when switching tabs.

7) Tests / QA
Add or update tests for:
- API:
  - customer detail response includes momentumScore and momentumBand when available.
  - missing or null score returns a safe shape the frontend can handle.
- UI:
  - a customer with a high score shows the correct number, band label, and color.
  - each band maps to the correct label and color.
  - customers with no score show the “— / Not yet calculated” state gracefully.
- NBA behavior:
  - ensure no regressions: this change must not alter score computation or NBA rules.

8) Output
When you implement this, please return:
1. Files changed (API + frontend)
2. The exact shape of the new customer detail response fields
3. Screenshots or a sible where AMs actually work the account, not just in backend or rep“no score yet” is detected)