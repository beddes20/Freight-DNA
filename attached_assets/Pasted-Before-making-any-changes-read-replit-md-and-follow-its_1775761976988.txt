Before making any changes, read replit.md and follow its development guardrails.

Also treat the documented “My Procurement — Lane Matching Contract (locked April 2026)” in replit.md as non-negotiable context. Do not regress any lane matching, normalization, procurement navigation, or task-opening behavior while doing this work.

I need you to debug and fix a real carrier-history / ranking failure in Lane Work Queue and Carrier Hub. This is not a greenfield feature. Assume the system is already supposed to rank carriers using:
- region/equipment fit
- exact lane history
- customer history
- prior outreach outcomes

Problem summary:
In Carrier Hub, I’m looking at Dhami Carrier LLC. The overview looks fine, but the platform is clearly not pulling history correctly.
Dhami runs a lot for us out of Arizona, including to Utah, Oregon, and Washington.
They do not show up in carrier lane search for Arizona -> Washington.
I created a new lane in Lane Work Queue for Phoenix, AZ -> Kent, WA, and Dhami still did not surface near the top.
Instead, 1 61 Inc popped up first.
No meaningful lane history is pulling in LWQ either.
This is badly wrong. Dhami should rank very high or first if our historical data is being matched correctly.

Objective:
Find exactly why Dhami Carrier LLC’s historical lane activity is not showing up in:
1. Carrier Hub history / lane search
2. Lane Work Queue history display
3. Carrier ranking for a Phoenix, AZ -> Kent, WA lane

Then fix the root cause and add regression coverage so this class of bug does not come back.

Important:
Do not paper over this by just hardcoding Dhami or blindly increasing one weight.
I want the actual history linkage / extraction / ranking logic fixed correctly.

Required investigation:
A. Verify the underlying history data exists
- Find Dhami Carrier LLC in the database using the real carrier record / carrier_id
- Pull historical loads / shipments / lane history tied to Dhami
- Specifically inspect Arizona-origin history, especially Arizona -> Washington, and also Arizona -> Utah / Oregon if those exist
- Show whether this history is actually present in the database and what fields it is stored under

B. Trace the history path end-to-end
Trace the exact flow from historical load data to:
- Carrier Hub history display
- Lane Work Queue history display
- Lane ranking candidate scoring

Check for issues like:
- carrier_id mismatch
- history joined by name instead of carrier_id
- origin/destination city/state normalization mismatch
- Phoenix vs metro vs ZIP vs region mismatch
- Kent, WA vs Seattle metro / Washington region mismatch
- normalized lane string mismatch
- history extractor using different lane formatting than ranking engine
- filters that accidentally exclude valid history rows
- stale / partial ingest logic
- query only looking at one lane format while history is stored in another

C. Debug the actual ranking for a concrete case
Use this exact example:
- origin: Phoenix, AZ
- destination: Kent, WA

I need you to inspect the real ranking path and answer:
1. Is Dhami present in the candidate set at all?
2. If not, why not exactly?
3. If yes, what score does Dhami get?
4. What score/signals caused 1 61 Inc to rank above Dhami?
5. What did the system think Dhami’s exact-lane-history signal was?
6. What did the system think Dhami’s customer-history signal was?
7. What did the system think Dhami’s region-fit signal was?

I do not want a vague explanation. I want the actual reason from the code and data.

D. Fix the root cause
Based on what you find, implement the proper fix.
Examples of acceptable root-cause fixes:
- normalize lane keys consistently across history extraction and ranking
- match history by carrier_id reliably
- fix city/state/region corridor matching
- include valid historical rows currently being dropped
- correct a broken SQL join / filter / aggregation
- correct exact-lane vs corridor-history scoring logic
- surface the missing history in Carrier Hub and LWQ

Examples of unacceptable fixes:
- hardcode Dhami
- manually reorder a specific carrier
- just increase one score constant without proving the data path is correct

E. Add ranking transparency while debugging
Add temporary or durable debugging visibility so we can prove why a carrier ranked where it did.
At minimum, I want the ability to inspect for a ranked lane:
- exact lane history count
- corridor / regional history count
- customer history count
- outreach history contribution
- final score
- reason Dhami was excluded if excluded

If there is already a “Why this carrier” mechanism, improve/reuse it.
If not, add a minimal debug-safe path so this is inspectable during QA.

F. Add regression tests
Add tests covering this class of issue, not just this one carrier.
At minimum:
1. A carrier with strong exact lane history should appear near the top for that lane
2. Historical rows with lane formatting differences (case, whitespace, punctuation, city/state formatting) should still match correctly after normalization
3. Carrier Hub history and Lane Work Queue history should reflect the same underlying history source for a given carrier/lane
4. A carrier with Arizona -> Washington history should not be outranked by a carrier with no relevant history unless another explicit score dimension clearly justifies it

Required workflow:
1. Start by listing the exact files you think are involved before changing anything
2. Identify overlap with any high-risk shared surfaces
3. Explain the current data path for:
   - carrier history in Carrier Hub
   - lane history in LWQ
   - ranking signal generation
4. Then implement the fix

Likely files to inspect:
- storage.ts
- routes.ts
- Lane Work Queue ranking/query code
- Carrier Hub history code
- any lane normalization / formatter helpers
- any shipment-history / history extractor logic
- any “Why this carrier” or ranking explanation logic

Deliverables:
1. Root cause summary in plain English
2. Exact files changed
3. What was wrong in data matching / ranking logic
4. What was changed to fix it
5. Test coverage added
6. A concrete proof using Phoenix, AZ -> Kent, WA showing:
   - whether Dhami now appears
   - where Dhami ranks
   - what history counts/signals are now being recognized
7. Confirmation that Carrier Hub history and LWQ history now reflect the same underlying history correctly

Success criteria:
- Dhami Carrier LLC historical Arizona outbound activity is actually visible to the system
- Dhami appears appropriately in Arizona -> Washington lane search / ranking
- Carrier Hub and LWQ both show usable history
- ranking reflects real historical performance rather than obviously wrong carriers surfacing first
- no hardcoded carrier-specific logic
- regression tests pass

Return format:
- What I found
- Root cause
- Files changed
- Fix implemented
- Tests run
- Proof on Phoenix AZ -> Kent WA

Do not stop at “I found the likely issue.”
I want the fix implemented, tested, and proven with the Dhami example.