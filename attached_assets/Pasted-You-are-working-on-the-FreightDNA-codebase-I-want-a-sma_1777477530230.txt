You are working on the FreightDNA codebase. I want a small, careful first upgrade pass on the Customer Quotes page based on the completed read-only audit.

IMPORTANT:
Do NOT redesign the whole page.
Do NOT change schema, migrations, packages, or backend quote business logic.
Do NOT change quote ingestion, autopilot, LWQ handoff, freight opportunity creation, or email pipeline behavior.
Do NOT split the giant page file in this branch yet.
This is a focused UX + performance pass only.

### GOAL

Implement ONLY these first-step improvements on Customer Quotes:

1. Reduce unnecessary always-on query work where it is safe.
2. Add visible sub-view presets for the most common rep workflows.
3. Make the default list orientation more action/aging-first.

### SCOPE

Approved changes:
- frontend query gating for lightweight supplemental panels where appropriate
- lightweight UI additions to the Customer Quotes page
- default sorting / filter preset behavior using existing query/filter infrastructure
- minimal tests for these exact changes if needed

Not approved in this branch:
- page-wide redesign
- backend endpoint redesign
- schema changes
- route changes unless absolutely minimal and already supported by existing filters
- breaking apart the 2,901-line page yet
- changes to quote lifecycle semantics

If any requested change would violate these constraints, STOP and ask me.

---

### STEP 1 — Read-only implementation plan first

Before editing anything, inspect the existing Customer Quotes code again and propose a precise implementation plan for ONLY these three items:

A) Query gating
- Gate `ActionQueueCard` and `NewContactReviewStrip` queries so they do not always fire unnecessarily on first paint.
- Use the lightest/safest gating mechanism available based on the existing page structure.
- If viewport-based gating is too invasive, propose a simpler “only after first interaction / only for eligible roles / only when visible section is expanded” approach.

B) Sub-view presets
Add a simple visible preset control near the top of the page using existing filter combinations only:
- My Open
- Stale
- Won This Week
- Lost
- All

These should map to existing filters/sort state, not require new backend endpoints.
The presets should feel like fast operating modes for reps, not saved views.

C) Default action-first sort
Use the existing list/filter/sort system so the default view emphasizes older active quotes first.
Do not alter won/lost historical browsing behavior unnecessarily.
Propose the exact default sort you think is safest based on the current code.

STOP after the plan and give me:
1. files you would change,
2. exactly how query gating would work,
3. exactly how the preset control would map to current filters,
4. the proposed default sort behavior,
5. any test updates you think are needed.

Do not edit anything until I approve.

---

### STEP 2 — After approval, implement ONLY the approved plan

Requirements:

1. Query gating
- Reduce initial unnecessary query load for:
  - `ActionQueueCard`
  - `NewContactReviewStrip`
- Preserve behavior once the user needs those sections.
- Do not break role-based visibility.

2. Sub-view presets
Add a small, obvious preset control near the top of the page for:
- My Open
- Stale
- Won This Week
- Lost
- All

Rules:
- use existing filters/state only
- make the active preset visually clear
- if a user manually changes filters afterward, it is okay for the page to fall out of a named preset state
- do not replace Saved Views; this is a lightweight rep workflow control

3. Default sort / action orientation
- Make the default list behavior prioritize actionable aging quotes more clearly.
- Keep this implementation conservative and easy to understand.

---

### STEP 3 — Tests and verification

Add/update only focused tests if needed for:
- preset → filter mapping,
- any changed default sort behavior,
- and any query gating logic that is practical to verify.

Do not write broad new E2E coverage if a focused test is enough.

---

### STEP 4 — Final summary

When done, output:
1. files changed,
2. how query gating now works,
3. the exact preset definitions,
4. old vs new default sort behavior,
5. tests added/updated,
6. confirmation that:
   - no schema/migrations/packages changed,
   - no backend quote lifecycle logic changed,
   - no autopilot/LWQ/email pipeline behavior changed,
   - no broad redesign or file split was done.

Stop there. No further changes.