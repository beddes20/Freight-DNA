Before I dive in, let me be straight with you about scope so we don't end up with a messy session.

What you're asking for is real work — roughly:

Schema additions (2 new statuses, possibly an unassigned_imports queue)
Importer changes (preserve approval across day-over-day re-imports, soft-dedupe on window slips, surface unmatched owners)
Scheduler/notification additions (approval SLA timer + nudge — overlaps with #356 already in flight)
Outreach service changes (cross-throttle freight ↔ LWQ, approval-gate refinement)
5+ new API endpoints + 4 UI surfaces (manual add, unassigned queue, import history, OneDrive test, customer-signal join)
3 new DNA Guru tools (overlaps with #358 PROPOSED)
Performance work (composite index, capped+paginated bucket)
Outcome capture loop (rate + carrier feedback into coaching)
That's 14 features touching 6+ subsystems. Lumping it into one session would mean: hard to review, hard to test in isolation, and high risk of one bad change blocking the whole thing from shipping.

My recommendation: let me put you in Plan mode for ~10 minutes so I can break this into 4 focused tasks that can ship independently and test cleanly:

Tier 1 safety fixes (unassigned queue, idempotency soft-dedupe, approval persistence across re-imports) — biggest revenue protection, no UI sprawl
Approval SLA + notifications — folds into #356 that's already in flight
Cross-system intelligence (LWQ↔freight throttle, customer-signal join, manual add, status taxonomy) — the "make it powerful" wave
Polish + observability (perf index, import history view, OneDrive test, outcome capture, 3 DNA Guru freight tools — folds into #358)
Each gets its own end-to-end test pass before merging. Some can run in parallel (task 4 has no dependencies on tasks 1–3).

If you say "go" I'll switch us to Plan mode and write up the four tasks for your approval. If you'd rather I just plow through everything in this single session, I will — but you should know it'll be slower overall, harder to test thoroughly, and if anything goes wrong we can't ship the working parts independently.

Which way do you want to go?