Use this exact prompt in Replit:

We need to fix the Lane Work Queue carrier suggestion ranking in the existing Value Truck app.

This is not a greenfield rewrite. Adjust the current ranking/scoring logic so the top of the list reflects recent proven execution, not stale eligibility.

Core problem
A carrier like 1 61 INC is appearing first or near the top on many lanes even though:

they are technically active in our system

but we have not run a load with them in about a year

That is not acceptable ranking behavior.

A carrier should not dominate the top of lane suggestions just because they are:

active

contactable

in the region catalog

or broadly compatible on paper

If they have not moved freight with us in a long time, they must decay hard in ranking.

Objective
Rework the lane suggestion ordering so that:

recent proven fit outranks generic fit

exact lane history outranks broad regional fit

customer history materially matters

stale carriers decay aggressively

active status is a gate, not a winning score signal

weak signals like “has email,” “contactable,” or “in region catalog” cannot force a carrier to the top

The end result should be:

lane suggestions feel obviously credible to an operator

stale carriers remain available as fallback options when appropriate

top slots are reserved for the strongest recent proven candidates

Product principles to preserve
These are non-negotiable:

Claimed fit and proven fit must stay distinct

Recent history matters more than static attributes

Carrier status still matters (active, inactive, flagged, do_not_use)

Fallback carriers should remain visible

The lane work queue should get smarter from history and recency, not just catalog presence

Do not collapse claimed and proven signals into one generic score.

What is likely wrong in the current logic
The current ranking appears to be overvaluing some combination of:

active status

region fit

equipment fit

contactability / has email

generic region catalog inclusion

stale historical volume without enough recency decay

And underweighting or failing to properly gate:

recent exact lane history

recent customer history

days since last executed load

recent executed load volume

stale inactivity penalties

That is why a stale carrier keeps surfacing at the top across many lanes.

Required ranking changes
1) Add a strong executed-load recency signal
Use last successful executed load with Value Truck as a major ranking factor.

Create or strengthen a signal like:

daysSinceLastExecutedLoad

This needs to meaningfully affect rank.

Suggested behavior:

0–30 days = strong positive

31–90 days = moderate positive

91–180 days = small positive / neutral

181–364 days = negative

365+ days = strong negative

Important:
If a carrier has not executed a load with us in 365+ days, they should generally not appear first on most lanes unless they have extremely strong recent exact-lane or customer-specific evidence that overrides it.

2) Exact lane history must outrank generic fit
For a given lane, recent exact lane history or normalized lane history should be weighted much more heavily than:

region fit

equipment fit alone

catalog presence

contactability

If Carrier A has recently run the exact lane and Carrier B is just a generic fit, Carrier A should rank above Carrier B.

3) Customer-specific history must materially boost rank
If a carrier has recently moved freight for that same customer, that should matter a lot.
If they have not moved freight for that customer in a long time, that signal should decay.

4) Active status must become a gate, not a winner
active should mean:

eligible to be considered

It should not mean:

likely top-ranked

Do not let active status overpower stale load history.

5) Add stale-carrier penalties
Add explicit stale-execution penalties:

no executed loads in 180+ days = meaningful penalty

no executed loads in 365+ days = severe penalty

This should apply even if the carrier is still active and contactable.

6) Reduce weak-signal influence
Lower the weight of signals like:

has email

contactable

in region catalog

broad region match

generic equipment compatibility by itself

These can help break ties, but they should not decide the top of the list.

Desired ranking priority order
Please rebalance the ordering so the practical priority is:

Recent exact lane history

Recent customer-specific history

Recent overall executed-load recency

Recent successful volume with us

Equipment / service compatibility

Regional fit

Outreach / contactability / catalog signals

If the current ranking already uses a weighted score, adjust the weights instead of rebuilding from scratch.

Desired behavior examples
Case A — stale active carrier
Carrier is:

active

contactable

in region catalog

no executed loads in ~1 year

no recent exact lane history

no recent customer history

Expected result:

should not rank near the top on most lanes

can still appear lower as fallback

Case B — proven incumbent
Carrier is:

active

has recent exact lane history

has recent customer history

last executed load within 30–60 days

Expected result:

should rank near the top consistently

Case C — new but plausible carrier
Carrier is:

active

no historical usage

good region/equipment match

contactable

Expected result:

should appear as an exploration/fallback option

should not outrank a strong proven recent incumbent

Ranking explanation / “why this carrier” updates
Wherever ranking reasons are shown or can be surfaced, update them to reflect the corrected logic.

For stale carriers, explanation should explicitly acknowledge staleness, for example:

“Active carrier, but no executed loads in the last 365 days”

“No recent exact-lane history”

“Shown as fallback based on region/equipment fit”

For strong carriers:

“Ran this exact lane 6 times in last 90 days”

“Recent history with this customer”

“Last executed load 12 days ago”

Do not let the UI imply a stale carrier is a top recommendation simply because it is in the region catalog.

Bench / top-list behavior
The top 20 suggested carriers should heavily favor:

recent incumbents

recent proven fits

lane/customer recency

carriers with credible current execution signals

Stale carriers should not occupy top positions by default.

If necessary:

keep them visible deeper in the list

or classify them as weaker suggestions

or let them surface only after stronger proven options

Implementation guidance
Reuse existing signals if present
Build on the signals we already have:

region/equipment fit

exact lane history

customer history

prior outreach outcomes

carrier status

Do not invent a separate disconnected ranking engine.

Centralize ranking logic
If helpful, add or update helpers like:

getCarrierLastExecutedLoadAt(carrierId)

getCarrierRecencyScore(...)

getCarrierExactLaneHistoryScore(...)

getCarrierCustomerHistoryScore(...)

applyStaleCarrierPenalty(...)

getCarrierRankingBreakdown(...)

Avoid duplicating logic across API and UI.

Preserve eligibility filtering
Still exclude or suppress carriers appropriately based on:

inactive

flagged

do_not_use

equipment incompatibility

missing required service fit

Tests required
Please add or update tests covering at least:

Recency decay
recent executed carrier outranks stale carrier

carrier with no executed loads in 365+ days gets strong penalty

Exact lane history
recent exact-lane carrier outranks generic region-fit carrier

Customer history
recent customer-history carrier outranks no-customer-history carrier

Weak signal suppression
has email, contactable, and region catalog alone cannot produce top rank

Stale active carrier case
active carrier with no executed loads in ~1 year should not show up first by default across representative lanes

Ranking explanation
stale carriers show stale-history explanation

strong carriers show exact-lane / customer-history explanation

Deliverables
Please return:

summary of the ranking changes

exact files changed

the old vs new ranking behavior / weighting logic

assumptions made about lane matching, recency windows, and fallback behavior

one before/after example for a representative lane

Important:

fix the real ordering problem

keep scope focused on the lane work queue ranking pipeline

do not do a superficial UI-only fix

make the top of the list feel trustworthy to a live operator

If tradeoffs come up, prefer the logic that rewards recent proven execution and aggressively demotes stale active carriers that only look good on paper.