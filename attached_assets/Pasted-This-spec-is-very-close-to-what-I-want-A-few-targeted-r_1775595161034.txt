This spec is very close to what I want. A few targeted refinements to keep NBA focused on BOTH growth and relationship depth, and on the right inputs:

1) Please explicitly incorporate Relationship Scorecard signals into Phase 1/2:
   - DEFEND / EXECUTE rules that can fire off:
     - multiple declining contact scores on an account
     - active regression alerts on key contacts
     - no new contact added or no relationship stage advancement in 30+ days on A-tier accounts
   - The same guardrails you already set should apply (data freshness, multi-signal requirement).

2) For every GROW rule, make the lever and relationship move explicit:
   - Each GROW card must state:
     - the Growth Lever being used (lane_id, site_expansion, contact_mapping, spot_to_contract, referral, market_intel, recovery, pipeline_action, qbr)
     - the relationship move implied (new contact, stage advancement, intro, QBR, etc.)
   - Don’t surface “quote more” or “reach out” without tying it to a specific lever and relationship play.

3) Gate GROW rules not just by action rate, but by relationship/CRM hygiene:
   - Only enable GROW rules for an account when:
     - contact scoring is current (no stale scores beyond our target)
     - at least 2 contacts are mapped for A/B accounts, or we explicitly flag single-thread risk
     - pipeline hygiene is acceptable (limited stale opps)
   - If data hygiene is weak, either:
     - suppress GROW cards, or
     - show a single “fix the data” card that directly ties to a growth outcome (e.g., map 1 more contact at this A-tier account so we can surface better GROW plays).

4) For Phase 1 and 2, please explicitly mark which NBA rules are:
   - primarily “protect/defend”
   - primarily “execute pipeline”
   - primarily “grow revenue”
   - primarily “deepen relationship”
   Some rules will cover more than one, but I want to see we have coverage across all four outcomes, not just activity generation.

5) Keep the guardrail that NBA must not surface:
   - admin-only reminders
   - generic “check-ins”
   - cards with no specific account, contact, and action
   - recommendations based on incomplete or stale data

Otherwise, this spec is exactly the trust-first NBA rollout I was hoping for. Please update the document with the refinements above and then propose the *smallest* Phase 1 set of rules you think we should actually build first.