This is an improvement, but it is not the final fix for the rep column.

The remaining problem is the most important one:

a large share of email-sourced quotes still show Unassigned because they route to non-AM/NAM mailboxes, shared inboxes, or ops users,

and that means the portlet still is not reliably showing the actual responsible person / operations owner for many rows.

I want a follow-up investigation + implementation plan specifically for this remaining gap.

Goal

Make the Rep column show the best actual responsible person for email-sourced quotes, including ops/shared-mailbox workflows, not just AM/NAM mailbox ownership.

What I want you to investigate

For the remaining email-sourced rows that still show Unassigned, determine:

which mailbox types they are tied to (shared inbox, ops mailbox, generic queue, etc.),

whether there is another reliable ownership signal already in the platform, such as:

assigned operations user,

conversation owner / assignee,

thread owner,

quote owner,

activity actor,

last responsible internal user,

sender mapping,

team/pod ownership,

which of those signals should be used as the next rep-resolution tier for this portlet,

and how often each candidate signal is populated on the currently unresolved rows.

What I need back before coding

Come back with:

the population breakdown of the remaining unresolved rows,

the strongest next ownership signals available,

the proposed final rep-resolution order,

and whether the display label should really mean “rep” or should be renamed to something like “owner” / “responsible” if that better matches the data.

Important

Do not regress the suppression behavior you just fixed.

Do not weaken customer-facing gating rules without explaining exactly why.

Keep the customer-name improvements in place.

Keep this scoped to the Customer Quotes portlet and its data path.

If the best answer is that this column should actually represent “responsible owner” instead of strict sales rep, say that directly and propose the cleanest implementation.