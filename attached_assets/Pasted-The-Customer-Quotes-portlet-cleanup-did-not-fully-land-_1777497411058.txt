The Customer Quotes portlet cleanup did not fully land correctly. Please treat this as a targeted bug-fix pass on the existing implementation — not a redesign.

What is still wrong

Rep column is still wrong

The portlet is still showing “Unassigned” for rows that should have a real operations/assigned user shown as the rep.

I want the rep display to reflect the best actual assigned user/operations owner available for the quote, not fall back to “Unassigned” unless there is truly no valid user/rep assignment.

Customer names are still not polished

The portlet is still showing raw/unpolished values like:

Ptandl

Blackbirdlogistics

Valuetruck

etc.

I want the customer column to display a clean, human-friendly canonical company name.

What I need you to do first

Before editing, inspect the actual data path feeding this portlet and identify:

which field(s) the current Rep column is using,

whether there is a better source for the assigned operations user / quote owner / responsible user,

which field(s) the current Customer column is using,

whether a better canonical customer/account/company display name exists elsewhere in the loaded context or joined records,

and exactly why the current implementation is still falling through to Unassigned and raw customer strings.

Fix requirements
Rep column

Find the best true display source for the assigned rep / operations user for each quote row.

Prefer the actual linked user/person record that represents the quote owner / assigned operations user if it exists.

Only show “Unassigned” when there is genuinely no valid assigned user/rep available after checking the best available sources.

If the current join path is wrong or incomplete, fix the query/context loading so the correct user name is available to the portlet.

Customer name

Replace raw/sluggified/compressed customer strings with the best canonical human-friendly customer/company display name available.

Prefer a canonical account/customer display name from the real customer record if available.

If the current source is a low-quality stored name, inspect whether there is a better normalized display field already in the data model or customer context.

Do not do blind cosmetic title-casing if that would damage legitimate names; prefer the best real canonical source.

Scope

Keep this isolated to the Customer Quotes portlet/query/display path.

Do not redesign the page.

Do not reopen the previously approved sort/title/filter decisions unless required by a real code conflict.

What to return before coding

Come back with:

the exact current source fields for Rep and Customer,

the exact better source fields you propose to use instead,

why the current implementation is still producing Unassigned and raw customer names,

the files you expect to change,

and whether this is a query/join problem, a mapping problem, or a display helper problem.

If the root cause is obvious once inspected, you can implement immediately after that explanation and return the final diff summary, screenshots, and tests run.