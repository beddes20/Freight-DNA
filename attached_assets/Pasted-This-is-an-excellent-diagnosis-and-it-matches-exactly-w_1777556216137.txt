This is an excellent diagnosis and it matches exactly what I’m seeing in the UI.

I agree with your conclusions:

the current Quote Requests tab is a classifier slice over email_conversation_threads,

updated_at is a row-last-touched clock, not an email-activity clock,

the “Updated Xh ago” label is misleading,

and only ~40% of quote-request threads are actually linked to quote_opportunities, which explains why it feels siloed.

My vision is for this tab to be the front door and workflow surface for inbound quote email, tightly tied into Customer Quotes and analytics, and ultimately into AI learning. Your breakdown of A–G is aligned with that.

Go ahead with Phase 1

Please implement Phase 1 — “Stop lying about freshness” as a single, surgical PR, with no schema changes beyond what you already outlined:

Stop using email_conversation_threads.updated_at for any user-facing freshness label in Conversations (especially the Quote Requests slice).

Use canonical email-activity timestamps instead:

derive last_email_at as MAX(email_messages.provider_sent_at) per thread,

backfill and then maintain last_incoming_at / last_outgoing_at where appropriate.

In the Quote Requests list row:

show customer-facing activity based on real email times, e.g.

“Customer last replied 4/10 9:14 AM” and

“We last replied 4/12 3:42 PM”
(exact copy is flexible; what matters is that it reflects real email events, not background jobs).

Add the regression test you described:

assert that the list’s last_email_at is within a small window of MAX(email_messages.provider_sent_at) for any returned thread,

and ensure we never again regress to using updated_at for freshness in Conversations.

Guardrails:

Keep this scoped to Conversations / Quote Requests freshness; do not change quote linkage, outcomes, or AI behavior yet.

No behavioral dependence on updated_at in user-facing labels after this change.

Any internal uses of updated_at for system jobs/denormalization are fine; they just must not drive the UI clock.

Next, plan Phase 2

After Phase 1 is implemented and validated, I want a detailed implementation plan for Phase 2 — “Close the leakage seam”, using your Option 1 bias (promote quote_opportunities as the system of record for email-originated quote requests, with a synthetic path for still-unlinked threads).

For Phase 2 planning, please:

spell out exactly how quote_opportunities and email_conversation_threads will be linked (FKs, backfill, denormalized fields),

define the per-row UX for “Quote #… / Create quote”,

and show how this will move the linkage rate from ~40% toward 90%+ while keeping the Capture Leak Queue as a backstop, not the primary surface.

Once Phase 1 lands and you bring back the Phase 2 implementation plan, I’ll approve the next step.