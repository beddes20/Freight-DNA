I want a small, safe enhancement in Available Freight to make each row easier to understand operationally.

Desired behavior

For each Available Freight row:

If there is an order number available from the unified Excel upload / ReplitDailyUpload.xlsx data, display it clearly on the row.

If the row was created from the Customer Quotes → Won quote → Available Freight hero loop and there is no order number yet, display a clear marker such as:

Won from Customer Quote

If both exist, show:

the order number as the primary identifier,

and a smaller secondary source marker like Won from Customer Quote.

Goal

I want users to immediately understand whether a freight row:

already has an operational order number from the uploaded freight sheet,

or is a newly created opportunity coming from the won-quote workflow.

Implementation guidance

Find the Available Freight row/card renderer and determine:

where the current identifiers on the row come from,

whether the order number is already present on the row model,

and what field from the Excel upload is the canonical order/load identifier.

If the order number is already available in the row data:

just surface it in the UI in a clear, consistent way.

If the order number is not yet threaded through but already exists upstream in the uploaded data:

minimally thread it through to the row view without broad refactors.

For rows originating from the won-quote / hero-loop path:

show a clear source marker such as Won from Customer Quote

but do not remove the existing quote linkage behavior.

Constraints

No broad refactor

No schema redesign unless absolutely necessary

No changes to core hero-loop logic

Keep this a small display/data-threading enhancement

Reuse existing canonical fields from the upload if possible

Guardrails

Add focused guardrails/tests that ensure:

Available Freight rows render the order number when present

Won-quote-created rows render the fallback/source marker when no order number is present

If both are present, both can be surfaced without ambiguity

Deliverables

I want:

exact files changed

the canonical field used for the order number

before/after behavior for:

a row with an order number

a row with no order number but created from Customer Quotes

a row with both

Desired outcome:
Available Freight rows should clearly show either the operational order number, or a clear “Won from Customer Quote” marker, so users instantly understand what they’re looking at.