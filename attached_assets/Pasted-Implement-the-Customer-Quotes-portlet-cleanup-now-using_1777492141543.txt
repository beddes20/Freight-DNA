Implement the Customer Quotes portlet cleanup now using the already-confirmed rules below. Do not reopen these decisions unless you find a real code/data conflict.

Confirmed scope

This is a targeted cleanup of the portlet on the Customer Quotes page. Keep it as a small, isolated task. Do not redesign the full page.

Locked decisions

Portlet title

Rename the portlet from “Quote Opportunities” to “Customer Quotes” (keep the count).

Default sort

Use quote_opportunities.requestDate as the canonical sort field.

Default sort for this portlet should be DESC (newest first).

Preserve the SLA workflow by keeping the existing pinned sort behavior for the My Open and Stale presets; this change should not break those preset-specific behaviors.

Records to exclude
Exclude:

carrier-classified rows,

unknown/unclassified rows,

the “Unknown — needs review” placeholder bucket,

orphan rows whose customerId does not resolve to a real customer record.

Customer name

Use formatCustomerName(quote_customers.name) as the customer display value.

Do not invent alternative formatting logic unless required for null safety.

Rep name

Prefer linked users.name,

then fall back to quote_reps.name,

then render “Unassigned”.

Implementation expectations

Inspect the current query/data source feeding the portlet and update it cleanly.

Keep changes isolated to the portlet/query/presentation layer unless a small shared helper adjustment is clearly better.

Do not introduce unrelated behavior changes.

Do not change the broader page analytics or filters unless required for consistency with the customer-quotes-only scope.

Before coding

Quickly verify:

exact files touched,

exact query/path feeding the portlet,

whether preset sort behavior is isolated from the default portlet sort.

If all of that checks out, implement directly.

After implementation, return:

files changed,

exact query/filter changes,

exact field mapping for customer name, rep name, and sort,

confirmation that My Open / Stale preset behavior was preserved,

screenshots or a brief UI summary of the final result,

tests run and results.