I need to combine two requirements into one low-risk implementation for Lane Work Queue:

PRIMARY REQUIREMENT:
ReplitDailyUpload from the Financials / Lane Analytics tab must be the source of truth for recurring lane generation.

SECONDARY REQUIREMENT:
If freight ownership mapping is incomplete or unreliable, do NOT hide recurring freight.
Instead, use a smart fallback that categorizes and filters recurring lanes by customer so leadership can still review and assign a specific customer’s consistent freight.

Please implement both together.

Requirements:

1) Source of truth
- Confirm exactly which table/file/model the recurring lane engine currently reads from
- If it is not explicitly using ReplitDailyUpload from the Financials / Lane Analytics flow, change it so that it does
- Do not leave source selection implicit or ambiguous

2) Org linkage
- Ensure ReplitDailyUpload data is correctly linked to the org/account scope used by the lane engine and the work queue
- If org linkage is currently weak, implement the lowest-risk reliable link

3) Field mapping
- Use the real ReplitDailyUpload field names and normalize them safely
- Support the discovered export shape:
  - Origin
  - Destination
  - Customer
  - Delivery date
  - Trailer type
  - Order
  - Carrier

4) Smart customer fallback
- If lane ownership cannot be confidently inferred, recurring lanes should still appear in the queue within valid org scope
- Add customer categorization/filtering so I can select a specific customer’s consistent freight
- Add an “All customers” default plus a customer dropdown populated from visible recurring lanes
- Do not hide lanes solely because owner mapping is missing

5) Queue behavior
- Keep org safety intact
- If hierarchy scoping exists, preserve it
- Within the allowed visible scope, let the queue be filtered by customer
- Show customer name prominently on each lane row/card
- Keep origin → destination, loads/week, equipment, assigned owner, and bench/contactability metrics visible

6) Assignment workflow
- I need to be able to filter to one customer’s recurring freight and assign those lanes to the right LM or AM
- No major redesign; use the existing assignment flow

7) Traceability / debug
- Add lightweight admin/debug visibility showing:
  - source = ReplitDailyUpload
  - upload/file id used
  - latest upload date used
  - rows scanned
  - recurring lanes generated
- Also show whether a lane is being shown by direct ownership mapping or customer fallback logic

8) Tests
- Add/adjust tests proving recurring lanes generate from ReplitDailyUpload-shaped data
- Add regression coverage for Pascal Case / spaced column names
- Add tests proving lanes still appear when owner mapping is missing
- Add tests for customer dropdown population and customer filtering
- Add tests proving assignment still works from filtered customer results
- Preserve org scoping tests

Output format:
1. CURRENT SOURCE PATH
2. CURRENT VISIBILITY LOGIC
3. CHANGES MADE
4. HOW CUSTOMER FALLBACK WORKS
5. TESTS ADDED
6. HOW I CAN VERIFY THE QUEUE IS USING LANE ANALYTICS DATA