Thank you for the prod-level forensic report, even though the conclusion is hard to hear.

I understand your findings as:

Randall Zercher and John Stone are real people with email history, but there are no saved contact rows for them in prod today, and no contact-audit table to prove they ever existed.

Production only has 17 saved contacts total, mostly demo/test/Mohawk-import rows.

Both prod and dev only have ~5–6 weeks of email history, and the oldest data is from mid‑February demo seeds, which strongly suggests that the production DB was reset / restored / reseeded around late March 2026, and only a subset of data (like Mohawk contacts) was brought back.

Armstrong’s real contacts and their rich fields were almost certainly part of the data that was lost at that time.

At this point I need help with a recovery plan, not just diagnosis.

What I need from you next

Confirm what recovery options exist for our production database:

Do we have any point-in-time recovery / snapshots / backups for prod from before the late-March reset?

Are there Replit-managed Postgres backups or checkpoints we can restore from or read from?

Propose concrete recovery paths, with tradeoffs, for restoring lost contacts (including Armstrong) and their history, such as:

Option A: Restore a full prod snapshot from before the late-March event (and what it would overwrite).

Option B: Mount or copy data from an older backup into a side database, and selectively export/import the contacts and related tables (and what risks that carries).

Option C: Any CSV/JSON exports you know of on your side that could be used to rehydrate contacts.

For each option, please describe:

exactly what data would be recovered,

exactly what recent data would be at risk or overwritten,

how long it would take,

and whether it can be done as a one-time, surgical recovery rather than a full rollback.

Constraints

I do not want anyone to run destructive operations on the current prod DB until we’ve chosen an explicit recovery path and I understand the tradeoffs.

Read-only analysis is fine; actual restores/merges should only happen after I give written approval on a specific option.

Desired outcome:
A concrete, step-by-step recovery plan that tells me if and how we can bring back lost contacts like the Armstrong org chart from an earlier snapshot/backup, and what we would give up in recent data if we choose that path.