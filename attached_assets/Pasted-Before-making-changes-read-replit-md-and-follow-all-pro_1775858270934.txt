Before making changes, read replit.md and follow all project guardrails.

Context:
We have Email Intelligence Layer v1:
- email_messages / email_signals tables and migrations
- outbound hooks from laneCarrierOutreach.ts
- inbound hooks + providerMessageId-based idempotent upsert
- node-cron scheduler for processing
- extraction pipeline (quote/signature stripping, non-throwing)
- structured intents for customer and carrier email flows
- NBA mappings and outcomeType wiring
- tests passing, app running cleanly

Right now, signals are mostly stored, not widely consumed.

Goal:
Implement Email Intelligence Consumers v1 so email signals actively influence:
1) account scoring inputs & explanations
2) account/customer NBAs
3) carrier intelligence suggestions (backend side)
4) carrier NBAs
5) win/loss evidence hooks

We are NOT rewriting the core score or NBA engines; we are plugging into them.

--------------------------------------------------
1. ACCOUNT SCORE – EMAIL SIGNAL INPUTS
--------------------------------------------------

Use customer-side email signals:
- meaningful_touchpoint
- pricing_request
- objection_* (budget, timing, service, competitor, internal_priority)
- service_complaint
- new_opportunity
- stalled_thread
- urgency_signal

Tasks:
1) Add helper:
   getRecentAccountEmailSignalSummary(accountId, windowDays)

   Returns something like:
   {
     touchpointsCount,
     pricingRequestsCount,
     objectionsByType: { budget, timing, service, competitor, internal_priority },
     serviceComplaintsCount,
     newOpportunitiesCount,
     stalledThreadsCount,
     urgencySignalsCount
   }

2) Integrate into account scoring:
   - treat touchpoints as a small positive indicator
   - treat service complaints as a small negative / risk input
   - optionally surface pricingRequests/newOpportunities as momentum indicators
   - keep effects bounded; no one email radically changes the score

3) Update score explanation/breakdown:
   - include human-readable items like:
     - “3 meaningful email touchpoints in last 14 days”
     - “1 service complaint detected in last 30 days”
     - “No recent email engagement signals”

--------------------------------------------------
2. ACCOUNT / CUSTOMER NBAs – EMAIL-DRIVEN
--------------------------------------------------

Use customer email signals to spawn/update NBAs.

Signals:
- pricing_request
- objection_*
- service_complaint
- stalled_thread
- urgency_signal
- new_opportunity

Tasks:
1) Define a mapping from intent → NBA:
   e.g., pricing_request → { nbaType: 'account_pricing_followup', outcomeType: 'Execute', urgency: 'high' }
   objection_service → { nbaType: 'account_service_objection', outcomeType: 'Protect' }
   etc.

2) Implement email-driven NBA creation:
   - when a new email_signal is stored and linked to an account/load/opportunity:
     - consult mapping
     - create NBA if appropriate

3) Dedupe:
   - one active NBA per (accountId, threadId, intentFamily)
   - repeated signals update timestamps/metadata instead of creating duplicates

4) Explanation:
   - store emailSignalId / emailMessageId / threadId
   - store a short explanation string used in NBA details:
     - “Customer requested pricing on [lane X→Y] in email on 2026-04-10”

--------------------------------------------------
3. CARRIER INTELLIGENCE SUGGESTIONS – BACKEND
--------------------------------------------------

Use carrier-side email signals to populate a suggestions table (or reuse if already created).

Signals:
- lane_offer
- capacity_available
- capacity_unavailable
- new_lane_preference
- new_equipment_or_region
- price_pushback
- service_issue
- soft_commitment
- hard_commitment

Tasks:
1) Ensure we have a staging table like carrier_intel_suggestions:
   - carrierId, orgId
   - sourceType ('email_signal', 'market_nba', 'manual_user', 'system_inferred')
   - emailSignalId (nullable)
   - suggestionType ('lane_preference', 'capacity_available', 'capacity_unavailable', 'equipment_capability', 'region_preference', 'price_sensitivity', 'service_risk', 'general_note')
   - payload JSONB
   - confidenceScore
   - status ('pending', 'accepted', 'rejected', 'auto_accepted')
   - createdAt, updatedAt, acceptedAt, rejectedAt
   - acceptedByUserId, rejectedByUserId
   - comment

2) Map signals → suggestions:
   - lane_offer/new_lane_preference → lane_preference suggestion (with origin, destination, equipment, cadence if available)
   - capacity_available → capacity_available suggestion (region/equipment/time window)
   - capacity_unavailable → capacity_unavailable
   - new_equipment_or_region → equipment_capability or region_preference
   - price_pushback → price_sensitivity
   - service_issue → service_risk
   - soft/hard_commitment → lane_preference/capacity_available with higher confidence

3) Dedupe:
   - avoid duplicate suggestions for same (carrierId, suggestionType, normalized payload, threadId)
   - if an accepted suggestion exists, skip new pending duplicates

4) Wire into:
   - email processing pipeline (scheduler and/or inbound path) so new signals immediately create/update suggestions.

--------------------------------------------------
4. CARRIER NBAs – EMAIL-DRIVEN
--------------------------------------------------

Extend existing carrier NBA logic.

Tasks:
1) Define mapping from carrier email intents → carrier NBA types/outcomeType/urgency.

Examples:
- capacity_available → nbaType: 'carrier_capacity_followup', outcomeType: 'Execute'
- lane_offer → 'carrier_lane_offer_followup', outcomeType: 'Execute'
- price_pushback → 'carrier_price_review', outcomeType: 'Protect'
- service_issue → 'carrier_service_issue', outcomeType: 'Protect'
- hard_commitment → 'carrier_commitment_execute', outcomeType: 'Execute'

2) Implement creation/update:
   - triggered when new carrier email_signals are processed.
   - link NBAs to carrierId, laneId/region, threadId.

3) Dedupe:
   - one active NBA per (carrierId, threadId, intentFamily)
   - repeated signals update metadata.

4) Explanation:
   - store emailSignal reference + short explanation:
     - “Carrier offered capacity on XYZ lane in email reply dated 2026-04-10.”

--------------------------------------------------
5. WIN / LOSS EVIDENCE HOOKS
--------------------------------------------------

Tasks:
1) Ensure email_signals can be linked to:
   - opportunities,
   - loads,
   - or at least account+lane in a way that’s recoverable.

2) Add helper(s):
   - getEmailSignalsForOpportunity(opportunityId)
   - getEmailSignalsForLoad(loadId)

3) On outcome changes (closed_won / closed_lost):
   - ensure we can query related email_signals later.
   - no need to materialize aggregates yet; just make sure joins are easy.

--------------------------------------------------
6. INTERNAL INSPECTION ENDPOINTS
--------------------------------------------------

Add internal-only endpoints:

- GET /api/internal/accounts/:accountId/email-signals
- GET /api/internal/carriers/:carrierId/email-signals
- GET /api/internal/opportunities/:id/email-signals

Return:
- list of email_signals with intent, createdAt, linked entities.

Protect with whatever internal auth mechanism exists (INTERNAL_SERVICE_TOKEN etc.).

--------------------------------------------------
7. TESTS
--------------------------------------------------

Add tests for:

- Account score:
  - summary helper returns expected counts.
  - score explanation includes email-derived bullets.

- Account NBAs:
  - pricing_request generates exactly one NBA per thread+account.
  - reruns don’t duplicate NBAs.

- Carrier suggestions:
  - lane_offer / capacity_available / price_pushback create correct suggestions.
  - dedupe works.

- Carrier NBAs:
  - capacity_available, service_issue, price_pushback generate appropriate NBAs.
  - dedupe works.

- Win/loss:
  - signals are retrievable for a won/lost opportunity/load after outcome is set.

--------------------------------------------------
8. DELIVERABLES
--------------------------------------------------

Return:
1) short summary of changes
2) files changed
3) schema updates (if any)
4) how account score consumes email
5) email-driven account NBAs mapping
6) email-driven carrier suggestions + NBAs
7) win/loss evidence hooks
8) dedupe rules
9) tests added/updated
10) assumptions / TODOs for v2