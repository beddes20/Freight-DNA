We have another Quote Requests trust bug.

What I’m seeing

On /quote-requests, when I click the Needs routing tab:

the tab pill shows a non-zero count (Needs routing (215)),

but the table area says “Nothing in the routing queue. Capture-first contract is healthy.”

and the page also shows “Something went wrong – Failed to load snapshot.”

This is a contradiction. The UI is again telling two different stories at once.
I need you to treat this as a truth / state-consistency bug, not a redesign task.

Your job

Trace the exact data sources behind all three UI elements:

the Needs Routing tab count,

the list/table rows when status=needs_routing,

and the snapshot/KPI header request that is failing.

For the current user/session and org, prove:

what query produces the 215 count,

what query produces the empty table,

what request is throwing “Failed to load snapshot,”

and whether all three are using the same filters/scope/date window or not.

Find the first real mismatch:
Possible causes include:

tab count is org-wide but list is user-scoped,

list is applying an extra hidden filter,

needs_routing mapping is inconsistent between count query and list query,

snapshot failure is causing the page to fall back into a bad empty state,

or the frontend is showing stale count state with fresh empty rows.

Fix it so the page becomes truthful:

If there are 215 Needs Routing rows in scope, the table must show them.

If the table is empty because of a filter/scope issue, the page must explicitly say why.

If snapshot fails, it must not create or mask a false empty-state in the queue.

Add guardrails/tests:

Needs Routing count and Needs Routing list must be derived from consistent filter logic.

A snapshot failure must not cause the table to imply “nothing in queue” unless the list query itself is truly empty.

If count > 0 and list = 0, the UI must explain the narrowing condition.

Deliverables back

Root cause in plain English.

Exact files/functions changed.

Before/after proof for the same page state:

tab count,

table rows,

snapshot behavior.

Constraints

Do not touch capture-first, webhook, classifier, routing_status semantics, or backfill unless you prove they are directly causing this exact mismatch.

This is a UI/data-consistency fix, not another plumbing rewrite.

I am not willing to accept another version of Quote Requests where the counts, tabs, and table all disagree with each other.