Thanks for the architecture proposal — this is exactly the level of honesty and detail I wanted.

I confirm the following:

1) List + drawer vs cockpit
- YES: keep the existing /quote-requests list + drawer for triage and quick preview.
- The new cockpit at /quote-requests/:id is an ADDITIONAL, dedicated decision surface, not a replacement.
- Deep links that currently open the drawer (e.g. ?quote=<id>) must keep working.

2) Schema additions
- YES: I’m OK adding winningPrice, competitorName, and lessonsLearned columns on quote_opportunities and adding the new status enum values (needs_clarification, ready_to_quote, no_quote) in a later phase.
- Please stage schema changes in the Phase 4 step you outlined, not in Phase 1. Phase 1 should only rearrange existing data.

3) Similarity scoring
- YES: deterministic scoring is exactly what I want for trust.
- Please implement similarity using geography + customer + equipment + time-window + distance-band.
- Do NOT put an LLM in the hot path for similarity; it can be used offline to propose features later, but not in the main comps grid.

4) Pre-existing failing tests
- Correct: test:freight-capture-funnel and test:lane-system-e2e are out of scope here.
- Do NOT try to fix them inside this Customer Quotes task.
- You can mark this work READY TO MERGE WITH UNRELATED BASELINE FAILURES if those suites are still red for the same reasons as the baseline.

5) Bake-window / trust layer
- YES: preserve all trust primitives that already exist on Customer Quotes (LiveSyncPill, OwnerScopePicker, EmptyStateRecovery, etc.).
- The new cockpit must not regress that trust layer.
- If there is a tradeoff, keep trust / correctness over visual changes.

6) Phasing
Your phase split is acceptable and I want to proceed that way:

- Phase 1 (NOW): Build the cockpit shell at /quote-requests/:id using EXISTING data only.
  - Layout only: top intake strip → left column [Email Intelligence placeholder + Pricing Decision using existing PricingRecommendationCard] → right column [Similar Quotes placeholder + Similar Freight placeholder] → bottom Outcome strip using the existing outcome endpoints/buttons.
  - Do NOT introduce new endpoints or schema in Phase 1.
  - The goal is a working multi-panel cockpit reading from current detail data, with clean loading/empty states and no regressions.

- Phase 2: Implement real Email Intelligence panel + Similar Quotes panel, using existing extraction/classifier outputs and new similarity logic.

- Phase 3: Turn quotePricingRecommendation into a comps grid and wire a richer Pricing Decision panel (editable quote, actions).

- Phase 4: Postmortem flow + new workflow states + schema changes.

- Phase 5: Dedicated cockpit tests and acceptance verification (including “rep can complete the loop without leaving the cockpit”).

Please start **Phase 1** now.

Phase 1 acceptance criteria:
- New route /quote-requests/:id renders a six-panel cockpit layout.
- All EXISTING data that currently lives in the drawer is surfaced in the appropriate panels:
  - Quote intake/summary
  - Existing recommendation card in the pricing area
  - Existing outcome controls
  - Placeholders for Email Intelligence, Similar Quotes, Similar Freight with clear “coming soon” or “data not wired yet” states.
- The list + drawer at /quote-requests continues to work exactly as before (no regressions).
- Typecheck and guardrails clean.
- A small test verifies that /quote-requests/:id renders and wires to the same underlying detail data as the drawer.

Once Phase 1 is merged and stable, we’ll move to Phase 2+3 as you proposed.

If you see a way to safely pull one small piece of Phase 2 forward into Phase 1 without risking scope creep, call it out explicitly before doing it. Otherwise, keep Phase 1 lean and focused on layout + existing data only.