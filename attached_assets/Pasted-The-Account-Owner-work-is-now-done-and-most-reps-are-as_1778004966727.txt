The Account Owner work is now done and most reps are assigned.

I want to tighten the email → customer quotes ownership mapping so it uses Account Owner as the default fallback for known customer accounts.

Goal

When inbound email / quote activity is matched to a known customer account, I want ownership to resolve from the account first — specifically from Account Owner — instead of ending up blank or relying on weaker rep fallback behavior.

This should make the Rep column in Customer Quotes more trustworthy and make shared/distribution customer emails route correctly without reopening the email-plumbing chaos.

Desired ownership precedence

I want the ownership logic to be:

Explicit quote/opportunity owner if already assigned

Exact contact-level owner if one exists

Exact shared/distribution customer email match if one exists

Known customer/account match → Account Owner fallback

Needs Routing / Unassigned only if there is truly no safe account owner

In plain English:

if we know the customer/account,

and that account has an Account Owner,

use that Account Owner as the fallback owner for the quote/email workflow.

What I need you to do

Inspect the current email/quote ownership path and verify whether it is actually using the new canonical Account Owner source of truth end-to-end.

I want you to trace:

inbound email customer/account match,

ownership fallback,

quote opportunity assignment,

and Customer Quotes Rep column display.

Update the fallback logic so it uses the canonical Account Owner field for known accounts.

Do not rely on stale duplicated owner fields or old rep-copy fields.

Use the CRM/account source of truth that we just unified.

Make sure this works for:

named customer contacts,

shared/distribution customer inboxes tied to the account,

and general known-customer matches where there is no explicit per-contact owner.

Preserve safety:

do not overwrite explicit quote owners,

do not reopen webhook / Graph / capture-first / classifier / backfill work,

do not change routing_status semantics unless absolutely required.

Validate with real examples:

one quote from a named customer contact,

one from a shared/distribution customer email,

one from a known customer account that previously would have shown blank / unassigned,

and prove all now resolve to the correct Account Owner fallback when appropriate.

What I want back

The exact ownership precedence now used in code

The files/functions changed

Confirmation of the canonical source of truth being used

Before/after proof that known customer emails now fall back to Account Owner

Any cases that still legitimately land in Needs Routing

Constraints

Keep this a focused ownership/fallback cleanup

Do not spin this back into a broad email-plumbing rewrite

The goal is trust: if we know the account and it has an Account Owner, the quote/email flow should use it

Desired outcome:
Known customer emails and quote requests fall back to the account’s Account Owner instead of staying blank, inconsistent, or tied to the wrong weaker fallback.