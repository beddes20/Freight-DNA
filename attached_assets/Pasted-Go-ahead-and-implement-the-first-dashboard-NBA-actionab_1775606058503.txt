Go ahead and implement the first dashboard NBA actionability improvement.

Goal:
Add a "Log Touch" quick-action to NBA dashboard cards so a rep can log the recommended outreach directly from the card, without leaving the dashboard.

What to build:
1. In client/src/components/NbaCard.tsx
   - add a new primary quick-action button: "Log Touch"
   - this should open a compact dialog for logging a touchpoint tied to the card’s company
   - keep existing Done / Snooze / Dismiss actions intact
   - "Done" should remain available for clearing a card after work was completed elsewhere

2. Create client/src/components/NbaLogTouchDialog.tsx
   - lightweight dialog component specifically for NBA card execution
   - show company name as read-only context
   - if card.contactId exists, preselect that contact
   - otherwise load contacts for card.companyId and let the rep choose
   - default touch type to "call" unless current app patterns strongly suggest a better default
   - include the smallest useful form: contact, touch type, optional vibe if required by current pattern, optional note, meaningful toggle only if already standard
   - on submit:
     a. POST to the existing /api/touch-logs endpoint using the same payload shape/pattern used elsewhere in the app
     b. run the existing invalidation utility used after touch logging
     c. PATCH /api/nba/cards/:id/resolve with { action: "actioned" }
     d. close dialog and remove the card through normal refresh/invalidation behavior

Constraints:
- Frontend only
- No API changes
- No backend/schema changes
- Reuse existing query/mutation/form patterns already used in customers.tsx and global-log-touch-button.tsx
- Keep the dialog compact and focused
- Do not redesign the whole card
- Touch only the minimum files needed

Likely files:
- client/src/components/NbaCard.tsx
- client/src/components/NbaLogTouchDialog.tsx

After implementation:
- run typecheck on changed files if possible, then project typecheck
- note any pre-existing unrelated baseline errors separately
- summarize exactly what changed and the new rep click path from card -> touch logged
