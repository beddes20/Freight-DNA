I’m nearly ready to push V1 of Lane Carrier Outreach (Task #148), but I want you to proactively sweep for remaining gaps before we merge.

Context:
- V1 includes: new tables (carriers, recurring_lanes, lane_carrier_interest, carrier_outreach_activity, feature_flags), NBA rule recurring_lane_capacity, CarrierOutreachPanel, lane_carrier_outreach_v1 feature flag, and org-scoped carriers + lanes.
- Recent fixes: isEligible flag on recurring_lanes wired through schema, migration, engine, and queries; retractIneligibleLanes implemented; NOT IN query converted to inArray; lane PATCH route hardened for cross-org safety; isAdmin guard in admin-carriers tightened to require a resolved user.

Your job:
Do a systematic bug / gap pass focused on correctness, security, and UX, and then make the fixes.

1) Schema and constraints
- Verify all new tables have:
  - correct primary keys and orgId usage
  - appropriate foreign keys (accountId, carrierId, laneId, ownerUserId, overseerUserId)
  - a unique index on lane_carrier_interest for (orgId, laneId, carrierId)
- Confirm nba_cards.laneId and carriers.orgId are correctly nullable / required and enforced in the schema.

2) Org and auth boundaries
- For every new route, confirm:
  - queries are always scoped by orgId
  - PATCH / ownership changes cannot cross orgs
  - admin-only routes actually check !!user && ADMIN_ROLES.includes(user.role)
- Double-check that no admin route fires before auth is hydrated, and that non-admin users can never see or mutate other orgs’ carriers, lanes, or bench rows.

3) Eligibility and engine logic
- Confirm recurringLaneEngine:
  - correctly computes eligibility and sets isEligible = true when a lane qualifies and false when it no longer does
  - uses retractIneligibleLanes for lanes that fall out of pattern
  - never leaves “zombie” recurring_lanes that are no longer eligible but still considered active
- Verify laneScoringService and carrierRankingService:
  - don’t explode on missing data
  - handle edge cases like zero history, partial account data, or no candidate carriers.

4) Feature flag behavior
- Confirm lane_carrier_outreach_v1:
  - gates all new UI surfaces (NBA rule, CarrierOutreachPanel, carriers admin tab)
  - is ON by default only in dev, and requires explicit activation elsewhere
  - is checked in both client (feature toggles) and server (API entry points).

5) NBA card behavior
- For recurring_lane_capacity cards:
  - ensure the card only appears for eligible lanes (isEligible true)
  - lane score badge always has a safe default and does not break on null scores
  - completion criteria (≥3 carriers contacted or hasPreferredCarrierProgram) correctly mark the card done and apply the 30-day snooze
  - cards are correctly scoped to org and user (owner/overseer vs other roles).

6) CarrierOutreachPanel UX and state
- Ensure the panel:
  - handles empty states cleanly (no carriers, no suggestions, no history)
  - allows adding a new carrier email inline and persists correctly to carriers with the right orgId
  - keeps lane_carrier_interest in sync when status is changed, and updates nextSuggestedFollowUpAt and timestamps appropriately
  - degrades gracefully when AI services fail (fit score, email draft, reply classify, follow-up suggestions) and shows a clear fallback message.

7) AI service error handling
- For aiOutreachService, carrierRankingService, laneScoringService:
  - wrap external model calls with try/catch
  - log errors in a consistent way
  - return safe defaults (e.g., neutral scores, empty suggestions, “unable to classify”) instead of throwing uncaught exceptions.

8) Admin-carriers and seeding
- Confirm:
  - seed-from-Excel respects orgId and cannot seed into a different org
  - admin-carriers list and edit views are fully gated by admin/director roles
  - non-admins never see any carrier catalog UI or API responses.

9) Tests / type safety
- Add or update tests wherever they’re missing for:
  - recurringLaneEngine eligibility transitions
  - retractIneligibleLanes behavior
  - lane_carrier_interest unique constraint and status updates
  - admin/auth guards on new routes.
- Fix any type errors or implicit any usage in the new files.

Deliverables:
- Implement all fixes you find that are clearly safe and low-risk for V1.
- For anything non-trivial or behavior-changing, leave a TODO with a short comment and include it in a short “known issues / phase 2” list at the end.
- Finally, output:
  - a concise bullet list of changes you made, grouped by area (schema, engine, API, UI)
  - a short “Known Issues / Phase 2” section.

Do not start a redesign. Focus strictly on gaps, correctness, safety, and V1 readiness.