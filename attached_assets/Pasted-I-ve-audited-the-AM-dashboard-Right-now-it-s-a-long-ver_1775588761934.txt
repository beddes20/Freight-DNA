I’ve audited the AM dashboard. Right now it’s a long vertical widget board (17 sections) with no clear “start here,” and it doesn’t match our playbook’s morning block and proactive-cadence model.

I want you to implement Phase 1 of a dashboard redesign: reorder and reframe the existing widgets without adding new endpoints.

Goals:
- Make the dashboard a guided morning workflow, not a menu
- Put “Do This First” actions above analytics
- Keep all current data sources, but change order, grouping, and collapsed states

Current key widgets (for AMs):
1) Hero Banner – greeting, touch streak, tasks due (/api/dashboard/briefing)
2) LM Check-In banner (/api/lm-daily-checks)
3) PTO Passoff banner (/api/pto-passoffs)
4) Today’s 5 priority accounts (/api/dashboard/todays-five)
5) Stale Accounts (/api/dashboard/stale-accounts)
6) Trending Up/Down accounts (/api/dashboard/trending-accounts)
7) Personal Activity tiles (/api/dashboard/personal-metrics)
8) NBA portlet (/api/next-best-actions)
9) Account Growth portlet (/api/growth-scores)
10) Relationship Freight section (/api/dashboard-relationship-summary)
11) Tasks section (/api/tasks)
12) Cold Contacts + Meaningful Overdue (/api/dashboard/cold-contacts, /api/dashboard/meaningful-overdue)
13) Opportunity Leaderboard (/api/dashboard/opportunity-leaderboard)
14) Churn Risk (/api/dashboard/churn-risk)
15) Internal Feed/Wins (/api/feed-posts)
16) 1:1 portlet (/api/one-on-one/action-items)
17) Team Directory (/api/team-members)

Phase 1 scope (reorder/reframe only, no new endpoints):

1) Above-the-fold “NOW” zone (next 2 hours)
- Keep Hero Banner at the very top.
- Move the NBA portlet directly under Hero Banner.
  - Default-expanded.
  - Show only the top 3 Critical/High items with full CTA buttons.
  - Add a “Show all” expander for the rest.
- Move Today’s 5 directly under NBA and relabel it “Today’s Priority Accounts”.
  - Keep existing data.
- Add an RFP deadlines strip just under Today’s Priority Accounts, using existing RFP data:
  - Any account with an RFP due within the next 7 days.
  - Inline strip, not a new full widget.
- Move Tasks due today just below the RFP strip:
  - Filter the existing Tasks section to only show tasks due today or overdue in this above-the-fold slot.
  - Keep the full Tasks widget elsewhere for full management.

2) “This Week” zone (below the fold, default-expanded)
- Group or position, without changing endpoints:
  - Stale Accounts
  - Cold Contacts
  - Meaningful Overdue
  - Opportunity Leaderboard (growth)
- You do NOT need to build the unified attention list yet in Phase 1.
  Just reorder so these four sit together as the “attention / proactive” cluster.

3) “Running Context” zone (collapsed or lower priority)
- Default-collapsed (or moved further down) for:
  - Personal Activity tiles
  - Trending Up/Down
  - Churn Risk
  - Relationship Freight
  - Account Growth analytics
  - Internal Feed
  - 1:1 portlet
  - Team Directory

Non-goals for Phase 1:
- Do NOT create new endpoints.
- Do NOT build new scoring or ranking logic.
- Do NOT redesign individual widgets yet; only change order, labels, and collapsed states.

Deliverable:
- Updated dashboard layout with:
  - Clear “Do This First” zone at the top
  - Today’s priority accounts and tasks visible above the fold
  - RFP deadlines visible in the “Now” zone
  - Analytics and reference widgets demoted to secondary/tertiary positions
- A short summary listing:
  - Files changed
  - New ordering of sections
  - Which widgets are default-expanded vs default-collapsed

Please keep the implementation minimal and focused on layout and visibility, using existing components and queries.