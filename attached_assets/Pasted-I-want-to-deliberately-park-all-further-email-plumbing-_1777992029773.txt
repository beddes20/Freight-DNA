I want to deliberately park all further email plumbing work for now and shift focus to higher‑leverage product improvements. The capture‑first contract, routing_status, Needs Routing, SLOs, drops table, and the Mine Only trust fix are now in place and validated. I’m satisfied with the current reliability level for email → Conversations → Customer Quotes as a foundation.

New directive:
Treat the email layer as “good enough for now” and do not propose or start any more ingestion, webhook, classifier, or backfill work unless I explicitly ask. From this point forward, I want you working on Product / UX / Coaching features that sit on top of the current pipeline.

Guardrails for email work (do NOT cross these without my explicit request)

No new changes to:

Graph/webhook integration

capture-first contract or DROP-GATE equivalents

routing_status schema/logic

classifier thresholds / LLM prompts

backfill/repair scripts

quote_pipeline_drops semantics

The only email-related work allowed is:

reading existing data (for analytics/UX),

or surfacing already-computed fields in the UI.

If you believe any further email plumbing work is necessary, stop and ask instead of starting it.

What I want you to do next instead

Use the existing, now-stable data to make Freight‑DNA more of an industry‑leading quoting cockpit:

Customer Quotes – Phase 1 (Queue + Response Time)

Implement visible response-time metrics per quote:

first response time (inbound → first reply),

quote turnaround time (inbound → first priced quote sent).

Add a response-time column + badges in the Quote Requests table and basic team KPIs (averages, % within 15 min, etc.).

Make this work for the existing quote_opportunities and email_messages data; do not add new ingestion.

Customer Quotes – Phase 2 (Playbook panel)

For a selected quote, add a right-hand Playbook panel that shows:

similar recent quotes on the same/nearby lane (OD, equipment),

recent win/loss and margin bands for those,

and a small “lane notes / playbook tips” area the rep can see while quoting.

Use only existing history data (no new external sources).

Customer Quotes – Phase 3 (Coaching & Map, PLAN ONLY)

Design (don’t code yet) a compact coaching/analytics layer:

response-time vs win-rate views by rep, lane, and equipment,

and a North America lane map with quote/win/loss overlays for last 30 days.

Output this as a clear plan and wireframe-level description; we will decide when to implement it.

For each of these phases:

Describe the user stories.

List the backend queries you’d add or reuse (against existing tables).

Describe the UI changes and interactions.

Call out any new indices or derived fields you need, but keep them read/query-focused, not new ingestion.

Deliverables back to me

A concise Phase 1 implementation plan that does not touch email ingestion, only derives response times from existing data.

A Phase 2 Playbook design that uses current quote history to help reps price faster.

A Phase 3 (map + coaching) plan, clearly marked as “plan only” for now.

Repeat back the new constraint in your own words: no more email plumbing changes unless I explicitly request them; focus now is on turning the existing data into a best-in-class quoting and coaching experience.