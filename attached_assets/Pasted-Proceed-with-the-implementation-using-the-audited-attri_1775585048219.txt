Proceed with the implementation using the audited attribution model, including Rule 2 (priority-ordered deduplication).

Approved model:
1. Explicit lane attributions are authoritative.
2. If a contact has no explicit lane attributions, fall back to lane/region strings and label the result as Estimate.
3. Deduplicate per company in relationship priority order:
   Home Run → 3rd Base → 2nd Base → 1st Base → unknown.
4. Per-contact displayed loads must be claimed loads after deduplication, so contact rows reconcile to the company total.
5. Keep Unattributed and Unworked as distinct buckets with distinct UI treatment and action labels.

Implement in this order:
1. Fix dashboard-relationship-summary to use explicit-vs-fallback as a true priority rule (no additive double counting).
2. Add per-company deduplication to dashboard-relationship-summary.
3. Sort contacts by relationship priority before deduplication in both dashboard and company endpoints.
4. Update the company relationship freight portlet to show claimed loads, not raw matched loads.
5. Add attributionSource to contact results and render a badge:
   - Explicit
   - Estimate
6. Replace the N+1 attribution query pattern with the bulk getLaneAttributionsByCompanyIds call.
7. Differentiate Unattributed vs Unworked in the UI.

Constraints:
- keep this batch focused on correctness and trust
- do not redesign unrelated screens
- preserve current visuals where possible except where clarity requires change
- add short explanatory tooltip copy where needed

Validation required:
A. Files changed
B. Before/after logic summary
C. Example company validation showing:
   - company total
   - per-contact claimed totals
   - no double-counting
   - correct relationship-level ownership for overlapping lanes
D. Dashboard summary validation showing no inflated totals
E. Any remaining known limitations
Then stop and wait for approval before adding “Suggest from Awards” or other UX enhancements.