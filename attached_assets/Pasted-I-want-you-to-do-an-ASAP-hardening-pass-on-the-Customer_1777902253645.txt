I want you to do an ASAP hardening pass on the Customer Quotes / Quote Requests pipeline.
This is now a trust-critical workflow. The SSE/live-sync fix helps page freshness, but it does not fix the deeper problem that valid quote emails can still fail silently before they ever become visible quote rows.

Treat this as one parent task with sub-tasks and work through it end-to-end until the quote pipeline is observable, recoverable, and materially more trustworthy.

Objective

I want the Customer Quotes process to become dependable enough that:

if a valid customer quote email arrives, it has the highest possible chance of appearing correctly,

and if it does not appear, we can immediately see where it failed and why.

I do not want silent failures anymore.

First: audit the actual current pipeline

Trace the end-to-end flow from:

Microsoft Graph webhook / delta sync

email_messages

inline classification / quote detection

quote extraction / parsing

opportunity creation

rep attribution

customer resolution

live-sync publish

Quote Requests page visibility / filters

Confirm every branch that can currently lead to:

ingested

skipped_unparseable

skipped_outbound

skipped_duplicate

low-confidence / classifier miss

attribution to wrong rep / no rep

customer unresolved / unknown

quote created but hidden by visibility filters

I want a concise internal spec or doc that maps the pipeline stages and all failure modes.

Build this in priority order
T1 — Quote pipeline observability

Add durable metrics and health instrumentation for every stage:

inbound email volume

quote-classified volume

parse success / failure

opportunity created

attribution success / unattributed

customer resolution success / fallback / unknown

publish success

visible-to-rep success if measurable

Add a quote-pipeline watchdog / health view / alerting logic for anomalies such as:

inbound customer email volume high but quotes created = 0

parse failure rate above threshold

unattributed quote rate above threshold

unknown-customer rate above threshold

classifier success rate suddenly dropping

I want these failures to be obvious, not something we discover from rep complaints.

T2 — No more silent skips

Every skipped/failure branch must write a durable, queryable reason code and enough metadata to investigate.

Build or improve a review queue for quote-capture failures / leaks so admins can see:

email/message id

sender

subject

received time

failure reason

confidence / extracted context

suggested next action

Make sure all failure branches land there when appropriate.

T3 — Recovery / replay tools

Add a clean way to reprocess failed quote candidates after fixes:

re-run classification

re-run parsing

retry opportunity creation

retry attribution / customer resolution

I want replay to be safe, idempotent where possible, and auditable.

T4 — Attribution and customer resolution hardening

Finish and/or verify the existing Customer Quotes trust fixes:

write userId correctly at ingestion

backfill orphan rep rows

implement display-time email-to-user resolver as primary rep path where needed

improve canonical customer-name lookup

reduce “unknown customer” / wrong rep visibility issues

This matters because a quote can technically exist but still be invisible to the correct user.

T5 — Better admin visibility

Create a practical admin/operator view for quote health that shows:

today’s inbound candidate emails

quotes created

parse failures

unattributed quotes

unknown customers

likely leaks

top failure reasons

This does not need to be pretty; it needs to be operational and useful.

T6 — Quote page freshness verification

Confirm that after the SSE fix:

successful quote creations publish customer_quote

/quote-requests invalidates and updates automatically

the page no longer needs manual refresh when the pipeline succeeds

But do not confuse this with pipeline success. Keep freshness verification separate from capture reliability.

Guardrails and tests

Add tests for:

each major success and failure branch

skipped reason persistence

reprocessing behavior

attribution fallback behavior

unknown customer handling

quote publish event on success

quote page auto-refresh on successful publish

Add guardrails if useful so future refactors cannot silently remove observability or bypass the failure queue.

Scope rules

Keep this focused on Customer Quotes / Quote Requests trust and reliability.

Do not redesign the whole UI.

Do not expand into unrelated email intelligence work.

Do not hand-wave with “AI improvements” unless they are tied to a measurable failure mode in this pipeline.

Deliverables

When complete, send back:

pipeline map,

failure-mode summary,

exact files changed,

metrics/alerts added,

failure queue / replay tooling added,

attribution/customer-resolution improvements made,

tests added,

and what failure modes still remain after this pass.

The goal is simple: if a rep says “this quote should be here,” we should either have it there, or immediately know exactly why it is not.