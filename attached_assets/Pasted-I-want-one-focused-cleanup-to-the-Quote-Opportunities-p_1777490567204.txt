I want one focused cleanup to the Quote Opportunities portlet on the Customer Quotes page.

Please make the portlet behave like a true Customer Quotes list:

Show only customer quotes in this portlet.

Exclude any non-customer opportunity records or other record types that do not belong in Customer Quotes.

Keep the surrounding page and analytics unchanged unless needed for consistency.

Polish the Customer display.

Render the customer/company name in a cleaner, human-friendly format.

Prefer the best available canonical display name from the customer/account record.

If the current source is a slug, lowercase key, compressed name, or email-derived fallback, replace it with a properly formatted display label whenever a better customer name is available.

Do not introduce fake title-casing that could damage legitimate brand formatting; prefer canonical stored names when available.

Add Rep visibility.

Show the assigned rep name clearly in the portlet.

Use the best display name available for the rep/user record.

If no rep is assigned, show a clean fallback like “Unassigned”.

Default ordering.

Sort the portlet by most recent quote request first, descending by the best request timestamp/date field.

This should be the default load order for the portlet.

Keep it clean.

Preserve existing filters/search behavior unless a change is required to enforce customer-quotes-only behavior.

Do not redesign the whole page.

Keep this as a targeted portlet/query/column polish pass.

Before implementing, please inspect:

the query/data source feeding the Quote Opportunities portlet,

how customer display names are currently derived,

how rep names are currently derived,

and which timestamp should be the canonical default sort field.

Then come back with:

the exact records that will be excluded,

the exact source fields you’ll use for customer name, rep name, and sort order,

and a brief implementation plan before coding.