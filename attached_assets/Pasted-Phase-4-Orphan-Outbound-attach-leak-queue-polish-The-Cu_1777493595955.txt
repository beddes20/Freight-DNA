Phase 4 — Orphan Outbound attach + leak queue polish

The Customer Quotes portlet cleanup task is complete and merged. Please now execute Phase 4 (Orphan Outbound attach + leak queue polish) as previously queued and accepted. Use the same patterns from Phase 2A/2B/3; do not reopen already-confirmed decisions unless you hit a real conflict.

Goals

Give Orphan Outbound leak rows a real resolution path: “Attach to quote”.

After attach, the row should naturally stop qualifying as a leak (no special-case hacks).

Maintain diagnostics ↔ queue lockstep via the same buildLeakCandidateIds-style chokepoint.

Extend analytics so the resolution-mix tile shows Attached as a fourth resolution type.

Polish the queue UX with the small fixes already identified (focus + aria-live).

Behavior (high-level, you can refine)

Attach action

New action only on Orphan Outbound rows in the leak queue: “Attach to quote”.

Action opens a candidate-quote picker scoped to that row’s customer:

show open / recent quotes for that customer (and any other safe heuristics you propose),

allow search by quote number and maybe lane text, but keep scope tight.

On confirm, attach the outbound email to the chosen quote using existing linkage primitives where possible (same family as how inbound emails are associated with quotes).

Data + audit

Use the same data principles as 2B:

single source of truth for linkage (no parallel flags),

rows fall out of the leak queue because they no longer qualify.

On successful attach:

write a quote_events row with a clear actor such as manual_leak_attach and payload including { source: "capture_leak_queue", messageId, triggeredByUserId, leakType: "orphan_outbound", targetQuoteId },

write a capture_leak_reviews row with decision = "attached" (or equivalent) so the review table also reflects the resolution.

Queue + analytics

Ensure Orphan Outbound rows that are attached are excluded by the same shared chokepoint the Missed Inbound path uses (no separate ad-hoc filters).

Update the leak analytics:

resolution mix tile gains Attached as a distinct resolution type,

aging buckets / trendlines include attaches in the “resolved” series.

UX polish already identified

Implement the tab/type snapshot fix you noted earlier so focus-after-action cannot land on the wrong tab when a mutation completes after a tab switch.

Add a lightweight aria-live="polite" announcement for queue updates so screen readers get a meaningful “Row resolved” style message after actions.

Keep the rest of the queue UX from Phase 3 (sticky header, aging chips, skeletons, empty state, toasts) as-is unless you see a small, clearly-better tweak.

Before coding

Please come back with a brief Phase 4 plan that includes:

exact data model / linkage you’ll use to attach an outbound email to an existing quote (tables + key fields),

attach flow UX (candidate list behavior, search, and error states),

how Orphan Outbound will stop qualifying as leaks after attach (what changes, where),

the new analytics shapes (API response fields and how Attached will appear),

tests you’ll add (service + route + UI/e2e).

Once I approve that plan, move directly into implementation and return:

files changed,

final behavior of attach + queue removal,

analytics changes,

UX details (screenshots or description),

tests run and results, including any new race-safety tests.