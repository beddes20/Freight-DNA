Goal: Make ReplitDailyUpload.xlsx the single source of truth for Financials, Available Freight, AND LWQ / Lane Work Queue.

Current state:
- Financials already uses ReplitDailyUpload.xlsx.
- Available Freight has its own importer/health surface and needs to be unified to the same upload.
- Lane Engine / LWQ currently shows source = financial_uploads and already computes generated lanes, but I want it explicitly tied to the same ReplitDailyUpload.xlsx pipeline as the source of truth. [file:75]
- The Excel file includes fields like customer, order, origin, destination, carrier, margins, order type, freight, surcharges, total revenue, carrier pay, carrier total, margin %, bill miles, loaded miles, etc., which should be enough to support financials + available freight + lane recurrence logic. [file:76]

Business requirements:
1. I only want to upload/paste ReplitDailyUpload.xlsx ONE time in ONE location.
2. That single ingest must power:
   - Financials
   - Available Freight
   - LWQ / Lane Engine
3. LWQ recurring-lane eligibility rule must change:
   - OLD: roughly 2x/week
   - NEW: a lane qualifies when we have moved it at least 6 times in a month
4. Build the lane logic off the unified upload, not a separate legacy input.

Please do the following:

1) Inspect and document the current pipelines
Map where ReplitDailyUpload.xlsx currently feeds:
- Financials
- Available Freight
- Lane Engine / LWQ
Document:
- current ingest jobs
- staging/normalized tables
- which routes/pages read from which tables
- where duplication/divergence exists

2) Design one canonical freight fact source
Create or confirm a single canonical normalized table/view, e.g. `freight_daily_upload_fact`, populated from ReplitDailyUpload.xlsx once.
This canonical layer must contain the fields needed for:
- Financials reporting
- Available Freight queue logic
- LWQ recurring-lane detection and scoring

3) Wire Available Freight to the canonical source
Update Available Freight so it reads from the unified fact table/view, not a separate importer-specific path.
Its import-health indicator should reflect the same unified upload job that feeds Financials.

4) Wire LWQ / Lane Engine to the canonical source
Update the Lane Engine / LWQ so its source is the same canonical ReplitDailyUpload ingest.
The engine should derive recurring lanes from the unified freight fact rows.

5) Update LWQ recurring-lane logic
Change lane eligibility from “2x/week” to:
- “moved at least 6 times in a month”

Please define this explicitly:
- lane key = origin city/state + destination city/state + equipment (unless current system already has a better canonical lane key; if so, document it)
- count only actually moved/booked loads, not quotes or open opportunities
- default window = rolling last 30 days unless there is already a calendar-month design constraint
- if a lane falls below threshold, use a short grace period or soft-retraction approach so lanes do not flap excessively (document chosen behavior)

6) LWQ outputs
Make sure LWQ can show:
- why a lane qualified (e.g. “8 moves in last 30 days”)
- last moved date
- customer(s) supporting the lane
- recent carrier history if already available
- confidence / eligibility explanation

7) Simplify upload UX
Ensure there is exactly ONE upload/paste location for ReplitDailyUpload.xlsx.
Available Freight and LWQ should both clearly show they are using the latest unified upload and timestamp.

8) Tests and validation
Add/update tests to prove:
- one upload updates Financials, Available Freight, and LWQ
- Available Freight rows and Financials metrics come from the same canonical row set
- LWQ lane qualification uses the new >= 6 moves/month rule
- a lane with 5 moves does not qualify
- a lane with 6 moves qualifies
- the lane explanation text reflects the new rule
- import-health/freshness indicators reference the unified ingest source

9) Deliverables
Return:
- architecture note for the unified pipeline
- canonical table/view name(s)
- files changed
- exact LWQ eligibility logic
- tests added/updated and results
- before/after explanation of where I upload the file and how all 3 surfaces update

Important constraints:
- Do not leave separate shadow importers behind for Available Freight or LWQ.
- Do not break existing Financials ingest.
- End state must be:
  “Upload ReplitDailyUpload.xlsx once → Financials, Available Freight, and LWQ all update from the same canonical data source.”