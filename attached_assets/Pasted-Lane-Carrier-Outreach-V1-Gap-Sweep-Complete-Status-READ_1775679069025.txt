Lane Carrier Outreach – V1 Gap Sweep Complete

Status: ✅ READY TO PUSH

What changed in this pass

- Fixed strict TypeScript error in laneScoringService.ts
  - Resolved TS2352 by safely casting ScoreFactors via unknown → Record<string, unknown>, returning us to the known 107-error baseline (no net new TS errors).

- Hardened lane_carrier_interest concurrency behavior
  - DB already enforced uniqueness for name-only rows via idx_lci_unique_name (partial unique on (lane_id, carrier_name) WHERE carrier_id IS NULL).
  - Drizzle schema now declares an equivalent partial unique index so schema matches actual DB constraints.
  - upsertLaneCarrierInterest now catches Postgres 23505 (unique_violation) on the null-carrierId INSERT path and retries as UPDATE, so concurrent requests merge instead of 500-ing.

- Confirmed NBA recurring_lane_capacity scope and safety
  - runPhase1EngineForOrg passes orgId into getEligibleRecurringLanes(orgId), and generateLaneCapacityCards only ever sees lanes from that org.
  - Feature flag lane_carrier_outreach_v1 is checked per-org; generateLaneCapacityCards returns [] early when disabled.
  - getEligibleRecurringLanes filters isEligible = true, and generateLaneCapacityCards also guards against isEligible === false as a belt-and-suspenders check.

- Verified outreach-log and admin boundaries
  - POST /api/lanes/:laneId/outreach-log:
    - Validates carrierIds/carrierNames length parity.
    - Validates every non-null carrierId against user.organizationId.
    - laneId is validated via getLaneWithAccessCheck, which is org-scoped.
  - Admin-only carriers:
    - Backend routes reject non-admin roles with 403.
    - Frontend admin-carriers only loads when !!user && ADMIN_ROLES.includes(user.role), so no admin traffic before auth resolves.

- Validated completion logic end-to-end
  - Outreach-log writes upsert bench rows, then recomputes carriersContactedCount using distinct (carrierId ?? carrierName).
  - When count >= LANE_CONFIG.completionCarriersContacted (3), recurring_lane_capacity cards are marked actioned and snoozed 30 days.
  - After snooze expiry, getEligibleRecurringLanes re-includes the lane; supersedePreviousNbaCards explicitly ignores recurring_lane_capacity, so supersession only applies to company-level rules.