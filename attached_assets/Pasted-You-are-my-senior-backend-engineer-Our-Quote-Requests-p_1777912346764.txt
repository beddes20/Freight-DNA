You are my senior backend engineer. Our Quote Requests pipeline is in a P0 state: inbound quote-request emails from Microsoft 365 are not showing up in /quote-requests for org da3ed822-8846-4435-bb13-3cc4bf26f71d, and we need the fastest safe path to: (1) fully restore ingestion, (2) recover the backlog, and (3) add guardrails so this can’t silently recur.

You must assume that production access will be handled by a human; your job is to produce code changes + exact runbooks, not to execute anything yourself.

Current state (assume all of this is true)
/quote-requests shows 0 Open Requests / 0 Auto‑Captured Today, despite normal customer email volume.

In production email_messages for this org over the last ~48–72 hours:
~8–9k rows with direction='outbound', almost all with empty from_email, subject, and body.

0 or 1 rows with direction='inbound' in that period.


Microsoft Graph webhooks and delta-sync logs show messages being fetched, and mailbox last_sync_at keeps updating.

Direction inference is currently:
```ts
const isFromMailboxOwner = fromEmail.toLowerCase() === mailboxEmail.toLowerCase();
const direction = isFromMailboxOwner ? "outbound" : "inbound";
```

Field extraction in the delta-sync layer looks like:
```ts
const fromEmail = msg.from?.emailAddress?.address ?? "";
const subject = msg.subject ?? "";
const bodyPreview = msg.bodyPreview?.slice(0, 255) ?? "";
const bodyFull = msg.body?.content ?? bodyPreview;
```
which silently writes empty strings when Graph omits fields.

The recovery script scripts/recover-todays-quote-requests.ts only backfills from email_messages where direction='inbound', so right now it has nothing to work with.

Your objective
Design the fastest safe end‑to‑end fix that:

Repairs ingestion so that new inbound emails are stored with correct direction, from_email, subject, and body.

Recovers the backlog since 2026‑05‑02 00:00 UTC into quote_opportunities for org da3ed822-8846-4435-bb13-3cc4bf26f71d.

Adds guardrails and monitoring so that:
we never again silently write empty‑content rows, and

we are alerted if inbound volume drops to zero while mailboxes still appear “healthy”.


What I want from you (deliverables)
Code changes (with snippets) to fix ingestion quickly, including:
Hardening the Graph field‑extraction layer: stop using ?? "" for required fields; instead, either fetch the full message (/messages/{id} with an explicit $select) or log+skip with an error if the payload is incomplete.

Ensuring there is exactly one canonical write path into email_messages for Graph messages, and that it correctly sets direction for both Inbox and SentItems. Call out any “rogue” inserts that hard‑code direction='outbound'.

A minimal but robust change you would ship in the first hotfix PR (not a giant refactor).


A precise hotfix rollout plan, with steps like:
Add temporary, privacy‑safe logging to distinguish “Graph payload missing fields” vs “our mapping wrong”.

Deploy instrumentation‑only, wait 5–10 minutes, interpret logs, then apply the smallest code change that guarantees:
we either persist complete messages, or

we don’t write a row at all and instead raise an error alert.



A production recovery runbook that assumes we now have good inbound rows again, including exact commands (but not actually running them) to:
Sanity‑check that in the last 15 minutes there are ≥10 direction='inbound' rows with non‑empty from_email, subject, and body.

Run scripts/recover-todays-quote-requests.ts with --org-id=da3ed822-8846-4435-bb13-3cc4bf26f71d --since-days=3, including a recommended dry‑run (--no-ai --limit=200) and the full run.

Verify in SQL that May 2, May 3, and “today” all show non‑zero email_sourced and healthy_inbound counts and that /quote-requests and any quote‑funnel health page show non‑zero captures.


Permanent guardrails & alerts, with concrete implementation details:
A small health check that compares:
“inbound signals from Graph in the last N minutes” vs

“rows in email_messages with direction='inbound' for the same window”.
If Graph activity > 0 and inbound rows = 0, flag an error.


A simple “empty content” alert: if more than X% of new rows in any 1‑hour window have empty from_email or subject, raise an alert.


Finally, a short “do this first” section (5–7 bullets) that an on‑call can follow immediately to get from “broken” to “fixed + backlog recovered” with minimal back‑and‑forth.

Assume all production access will be run manually by an engineer using the commands you specify. Do not run any commands yourself; just provide the smallest, clearest set of code changes and shell/SQL snippets that will fix this completely and quickest while staying safe.

