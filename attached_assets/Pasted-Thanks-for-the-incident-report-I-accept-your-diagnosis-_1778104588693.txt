Thanks for the incident report — I accept your diagnosis that:

the 64 contacts rows in prod were never bulk-deleted, and

most of the “contacts I remember” are actually account_contact_suggestions, and

the “fake customers” are email-derived stub companies from the auto-create pipeline, not the rollback.

I want to stay in read-only mode for one more step.

Step 1 — Read-only stub-company view (no writes)

Please implement a read-only admin view that shows:

all companies in prod that:

have 0 real contacts,

have no owner,

have no industry set,

and have ≥1 inbound email tied to them

plus:

created_at,

last inbound email date,

counts of related email_messages, quote_opportunities, and freight_opportunities if any.

This should be an admin-only page (e.g. /admin/email-derived-companies) behind whatever role gate you already use for admin consoles.

Important: this view must be read-only — no merge/archive/delete actions yet.

Step 2 — Design-only for contact promotion (no writes yet)

Separately, I want a design-only proposal (not implemented yet) for a safe:

“Promote suggestions to contacts” path that:

lets an admin select suggested contacts for a given company

and turn them into real contacts rows in a controlled way

with clear audit and an easy way to roll back a bad promotion.

For now, I only want:

the UX concept,

the proposed API shape,

and the guardrails you would add.
Do not implement the write path yet.

No backfills / no scripts

Please keep:

backfill-incident and any similar workflows disabled, and

do not run any repair or merge scripts against prod.

After the stub-company view is live in dev and you’ve shown me examples from prod (Armstrong and a couple of others), I will decide:

how we want to treat email-derived companies, and

whether to move ahead with a controlled bulk contact-promotion tool.

Desired outcome:

I can see exactly which “fake customers” and “suggested contacts” we actually have in prod,

without any additional writes, and

we design a safe path forward before touching production data.