Follow‑up task #1: tighten pickup rules for the Available Freight tab.

Product decision:
For the Available Freight tab, we want a **strict** definition of “past”:
- Any load with pickup date **before today** must be excluded by default, regardless of status.
- This overrides the Phase B1 “soft‑overdue” carve‑out for yesterday. Yesterday should NOT appear in Available Freight, even if the status is still open.

Implementation requests:
1) Update pickup freshness logic
- In shared/pickupFreshness.ts, adjust shouldHideForPickup for scope "actionable" so that:
  - freshness == "past_recent" AND daysSincePickup >= 1 returns true (hidden) regardless of status.
  - In other words, yesterday and older are hidden in Available Freight; the soft-overdue window should not keep yesterday visible.
- Keep the "all" and "recent" scopes as they are, so other views can still see soft‑overdue if needed.

2) Make Available Freight use the strict “actionable” gate
- Confirm the Available Freight server route still uses pickupScope="actionable" by default.
- Ensure the client default keeps pickupScope="actionable" for the main operational view and both reset buttons.
- If we need a soft‑overdue view in the future, that will be a separate tab or explicit pickupScope option, not the default.

3) Tests
- Update or add tests in:
  - shared/__tests__/cockpitTeams / pickupFreshness area if relevant
  - client/src/lib/__tests__/cockpitBuckets.test.ts
  - tests/cockpit-hardening.test.ts
- Explicitly cover:
  - yesterday + open status => EXCLUDED from Available Freight
  - same exact row visible when pickupScope="all" (history view)
  - midnight rollover still behaves as expected in CT

4) Verification
- Re‑run the cockpit-hardening test suite and show the updated assertions relevant to yesterday exclusion.
- Confirm that no other behavior (today, tomorrow, stale toggle) changed unintentionally.

Important:
Do NOT broaden scope. This task is only about making “yesterday and older” strictly excluded from Available Freight’s default view while preserving other scopes for future history/exception views.