I want to improve the main dashboard for NAMs and AMs so it becomes the clearest possible daily operating tool for account owners.

Do NOT start coding yet.

First, read the current NAM/AM dashboard/home page and evaluate only the top part of the page — the first screenful plus the next 1–2 sections below it.

Your goal is to determine whether the dashboard immediately answers these 3 questions for a NAM/AM:
1. What needs attention right now?
2. What should I do next?
3. Which accounts are moving, slipping, or blocked?

Please inspect the current render order, hierarchy, and purpose of the top dashboard sections, especially:
- alerts / banners
- hero / briefing / forced focus
- persistent NBA cards
- legacy next-best-action surfaces
- tasks due / overdue items
- priority account lists
- any early summary widgets or tiles

Then return a short product/UX audit with exactly these sections:

1. Current top-of-page order
2. What is helping the rep act quickly
3. What is creating noise, duplication, or choice overload
4. The single best top-of-dashboard layout for NAM/AM workflow
5. The smallest implementation we should make first
6. Exact file(s) that would need to change

Guiding principles:
- This dashboard should act like a flight deck, not a report.
- Top of page = act now.
- Middle = steer today.
- Bottom = diagnose and reflect.
- The system should make prioritization for the rep, not ask the rep to manually synthesize 5 competing priority systems.
- Persistent NBA cards and critical alerts should be treated as core daily-use surfaces.
- If two sections are trying to answer the same question, tell me which one should win and which should be demoted, merged, or removed.

Constraints:
- Ground everything in the current code and current UI only.
- No speculative redesign.
- No backend/schema/API changes unless absolutely necessary.
- Focus on the highest-value change we can make fast.

At the end, give me:
- your recommended new top-of-page section order,
- the first implementation to make,
- and a copy-paste build prompt I can use immediately.