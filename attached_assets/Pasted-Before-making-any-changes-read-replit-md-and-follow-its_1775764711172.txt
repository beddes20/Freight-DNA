Before making any changes, read replit.md and follow its development guardrails.

Also treat both locked sections in replit.md as non-negotiable:
- My Procurement — Lane Matching Contract (locked April 2026)
- Carrier History & Ranking Contract (locked April 2026)

I need to improve the carrier ranking logic in Lane Work Queue so it matches the real business intent better.

Current issue:
The system now correctly handles exact lane history, but I do NOT want ranking to rely too narrowly on exact string lane history.
The desired behavior is broader and more operationally useful:

Business intent:
1. A carrier with nearby origin + nearby destination history should rank very high, even if it is not the exact same city pair string.
2. A carrier with same state-to-state history should outrank a carrier with no history.
3. A carrier we have actually run freight with should generally beat a carrier we have never used, unless there is a very strong reason otherwise.
4. A no-history carrier like 1 61 Inc should not appear ahead of a carrier with meaningful Arizona -> Washington history just because the history is Phoenix -> Everett, Phoenix -> Tacoma, or another nearby WA destination instead of Kent exactly.

Desired ranking tier philosophy:
Rank history tiers in this order:
1. Nearby lane history: carrier has historical loads within ~100 miles of the requested origin AND within ~100 miles of the requested destination
2. Exact city/state lane history
3. Same state-to-state history
4. Broader region/corridor history
5. No company history / fit-only suggestions

Important business rule:
Any carrier with real company history in the lane area or state pair should rank ahead of a carrier with zero historical loads with us, unless an explicit suppression or hard disqualifier applies.

Goal:
Refactor ranking so nearby-lane and state-pair history are first-class signals, not afterthoughts.
I want this to reflect how operations actually thinks:
- “Have they hauled this exact thing?”
- “Have they hauled very close to this?”
- “Have they hauled this state pair a lot?”
- “Have we ever actually run with them?”
That matters far more than a cold-fit carrier with no history.

Implementation requirements:

A. Investigate current ranking tier logic
Show me the current ranking order / fitScore behavior and explain:
- where exact lane history sits
- where region history sits
- whether same-state-pair history is already represented
- whether nearby origin/destination matching exists today
- where no-history carriers can leapfrog historical carriers

B. Add nearby-lane logic
Implement a nearby lane history tier based on geographic proximity.

Desired behavior:
- requested origin within ~100 miles of historical origin
- requested destination within ~100 miles of historical destination
- both ends should matter

This should count as stronger than generic region/corridor history and stronger than no-history suggestions.
If exact city/state history also exists, exact can remain top tier or tie with nearby depending on your implementation, but nearby must be a strong top-ranking signal.

Use a clean, reusable approach:
- if lat/lng or geocoding helpers already exist, reuse them
- if they do not exist, implement a practical approximation based on known city/state coordinates or a lightweight distance utility
- do not introduce a huge external dependency if a simple internal utility works

C. Add same-state-pair history as a real tier
A carrier with meaningful Arizona -> Washington history should outrank a carrier with no company history, even if the exact destination city differs.
Example:
- Phoenix, AZ -> Kent, WA request
- carrier has Phoenix -> Everett, Tacoma, Spokane, or other WA history
That carrier should be ranked above a no-history carrier.

This state-pair history should be stronger than generic region fit and much stronger than no-history suggestions.

D. Add company-history-first behavior
If we have ever run loads with a carrier and they have lane-relevant history, that should strongly matter.
I do NOT want “never-used” carriers surfacing above carriers we actively use unless there is a clearly documented reason.

At minimum:
- any carrier with exact, nearby, or same-state-pair history should rank above carriers with zero company history
- no-history carriers should be clearly tagged as such in ranking transparency
- if a no-history carrier still ranks high, the reason must be inspectable

E. Update ranking transparency / Why this carrier
Expose enough detail so we can understand the new logic.
For each ranked carrier, I want visibility into:
- exact lane history count
- nearby lane history count
- same state-to-state history count
- broader region history count
- whether the carrier has any company load history at all
- final ranking tier
- final score
- any suppressions / deductions

If there is already a “Why this carrier” object, extend it.
Do not create a second conflicting explanation path.

F. Preserve locked invariants, but evolve them correctly
You may need to update the Carrier History & Ranking Contract in replit.md to reflect the new intended order.
Right now “exact > similar > region > none” may be too simplistic for the real business need.
Propose and implement the new durable tier order, then document it in replit.md.

The likely new order should be something like:
- exact or near-exact / nearby
- same state-pair
- region/corridor
- none

But use the cleanest terminology based on the code.

G. Tests
Add regression tests for this exact business rule.
At minimum:
1. A carrier with nearby-lane history within ~100 miles on both ends ranks above a no-history carrier
2. A carrier with same state-to-state history ranks above a no-history carrier
3. A carrier with exact or nearby Arizona -> Washington history outranks a carrier with zero history for a Phoenix, AZ -> Kent, WA request
4. Ranking transparency clearly identifies whether the match is exact, nearby, state-pair, region, or none
5. Existing exact-lane ranking behavior does not regress
6. No-history carriers can still appear, but only below historically relevant carriers unless explicitly justified

H. Proof case
Use this exact business example during validation:
- requested lane: Phoenix, AZ -> Kent, WA
I want proof of how the system ranks:
- Dhami Carrier LLC
- any other carriers with Arizona -> Washington history
- any no-history carrier like 1 61 Inc

The proof should make it obvious that a carrier with real Arizona -> Washington history will beat a never-used carrier.

I. Guardrails
Do not hardcode Dhami.
Do not special-case one carrier.
Do not just shuffle scores blindly.
Implement a general ranking improvement based on real company history relevance.

Required output:
- What I found in the current ranking logic
- Proposed new ranking tier order
- Exact files changed
- What logic was added or changed
- Tests added/updated
- Proof on Phoenix, AZ -> Kent, WA
- Any replit.md contract updates made

Success criteria:
- nearby historical lanes matter
- same state-to-state history matters
- no-history carriers do not jump above relevant historical carriers
- exact-lane history still works
- ranking explanations make the result understandable
- tests pass