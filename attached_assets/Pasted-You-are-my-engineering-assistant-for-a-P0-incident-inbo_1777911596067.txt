You are my engineering assistant for a P0 incident: inbound quote-request emails are not appearing in the /quote-requests UI for org Value Truck (da3ed822-8846-4435-bb13-3cc4bf26f71d). The system is fetching messages from Microsoft 365, but production data shows almost no direction='inbound' rows and many outbound rows with empty from_email / subject / body.

I already have a detailed incident analysis and recovery plan written up. I want you to refine it and turn it into a final, polished runbook, but you must follow these constraints exactly:

Treat this Replit environment as dev only. Do not run any commands or scripts against production.

Do not fabricate or use any production DATABASE_URL.

You may read and reason about code and scripts (like scripts/recover-todays-quote-requests.ts, graphWebhook, mailboxDeltaSyncService, etc.), but your output must be documentation only – shell commands and SQL that a human can run later.

Your tasks:

Take the incident runbook I paste below (it starts with “Quote Requests Pipeline — Incident Runbook”) and keep all of its technical content: script semantics, suspected root causes, file/function names, SQL, and shell commands.

Rewrite it as a clear, concise, production-ready runbook with these sections:
Overview (1–2 short paragraphs).

Script behavior & assumptions.

Root-cause hypotheses (ingestion layer).

P0 fix task (ready to paste into our tracker).

Production recovery runbook (step-by-step, with exact commands).

Engineer checklist (8–10 bullet points to tick off during execution).


Make the wording tighter and more readable, but do not drop any important detail (especially constraints like “run recovery only after inbound rows exist again” and “script is idempotent on (org, source='email', sourceReference)).

Call out any safety improvements you think we should add (e.g., extra read-only sanity queries before running recovery, clearer stopping conditions).

Again: you are only editing and improving the runbook; do not execute anything.

Here is the runbook text to refine (leave it inside a code block in your answer so I can copy it easily):

text
[PASTE THE FULL “Quote Requests Pipeline — Incident Runbook” TEXT HERE]  