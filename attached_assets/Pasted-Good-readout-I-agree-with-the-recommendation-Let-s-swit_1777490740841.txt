Good readout — I agree with the recommendation.

Let’s switch to plan mode for the next work, with Orphan Outbound attach as the priority next phase.

Before implementation, I want a plan that covers two items:

1. Phase 4 — Orphan Outbound attach

Please draft the implementation plan for adding a real resolution path for Orphan Outbound rows.

Goals:

allow an orphan outbound email to be attached to the correct existing quote opportunity,

make the row stop qualifying as a leak once attached,

preserve auditability,

keep diagnostics counts and queue rows in sync through the same shared chokepoint approach.

In your plan, please cover:

source-of-truth data and linkage strategy,

how the user picks the target quote,

whether attach should support only existing quotes or also a fallback create path,

audit event shape,

stale-row / race behavior,

route shape,

UI interaction in the queue,

tests.

Also include the small polish follow-up you noted:

snapshot tab/type alongside focusAfterIndex so focus cannot jump to the wrong tab after a mid-flight mutation,

and evaluate whether a lightweight aria-live improvement should ride along.

2. Customer Quotes portlet cleanup

I also want one targeted cleanup to the Quote Opportunities portlet on the Customer Quotes page.

Please inspect the current query/data source and propose the cleanest way to make it behave like a true Customer Quotes list:

show only customer quotes in that portlet,

polish the customer/company display name so it uses the best human-friendly canonical name available,

add the rep name clearly,

default sort to most recent first using the best request timestamp/date field,

keep the rest of the page unchanged unless required for consistency.

Before coding, come back with:

the exact record types that would be excluded,

the exact fields you’d use for customer display name, rep display name, and default sort,

whether the portlet title should remain “Quote Opportunities” or be renamed to better match the new scope,

and whether this should be implemented as one task or two separate tasks.