We finally have a concrete trust bug. I need you to fix it permanently, not patch around it.

Problem

Quote Requests / Customer Quotes is lying to users.

Production data exists for today. The /api/customer-quotes/list endpoint returns real rows when queried without the bad scope, and quote opportunities are being created correctly. But the UI can still show:

“No matches for the current filters”

0 open requests / 0 auto-captured today

while:

the Customer Quotes badge still shows 99+,

snapshot/KPI values still show org-wide activity,

and live production rows exist for today.

You already proved the likely cause:

mineOnly=off → rows return

mineOnly=on + no quote_reps mapping for the user → total 0 because applyMineOnly injects a fake no-results rep id / equivalent no-op-to-zero behavior

This is now a product trust bug, not an ingestion bug.

What I want fixed
1. Make Quote Requests impossible to lie

A user should never see a generic empty state when the real issue is:

Mine Only is on but they are not mapped to a quote rep

a saved view restored an invalid/restrictive filter state

filters exclude real rows while snapshot/KPIs are still org-wide

If there are rows in scope at the org level, the system must not present “no matches” as though there is simply no work.

2. Fix Mine Only correctly in both backend and frontend
Backend

In the server-side mineOnly application logic:

Do not silently force a zero-row result when the current user has no quote_reps mapping.

Return structured truth instead. For example:

myRepId: null

mineOnlyApplied: false

mineOnlyRequested: true

warningCode: "NO_QUOTE_REP_MAPPING"

If you believe org-wide rows should still be returned when mineOnly cannot be applied, do that.
If you believe the endpoint should return an explicit warning state instead, that is fine too.
But under no circumstances should it silently manufacture a fake empty result.

Frontend

In Quote Requests:

If Mine only is on and the user has no quote rep mapping, replace the generic empty state with an honest, actionable state:

Title: “Mine only is on, but you’re not mapped to a quote rep”

Body: explain why the personal queue cannot be shown

Primary action: Show all

Secondary/admin hint if appropriate: “Ask an admin to map your user to a quote rep”

If a saved view or restored page state silently turns Mine only on, surface that explicitly in the UI.

3. Reconcile list truth with snapshot truth

Audit the Quote Requests page so the list view and KPI/snapshot layer cannot drift into contradictory stories.

I want explicit rules for:

whether KPI tiles are org-wide or filtered,

whether badge counts are org-wide or filtered,

whether “No matches” means:

no rows at all,

no rows for current filters,

or no rows for current user scope.

If the tiles are org-wide and the list is filtered, the UI must say so clearly.
Do not allow a state where KPI tiles imply active work but the list silently looks empty with no explanation.

4. Audit saved view / restored filter behavior

Check whether:

a saved view,

local UI state,

default page params,

or server-provided defaults

can restore mineOnly=on or another restrictive scope without the user realizing it.

If so, fix it.

I want:

a clear source of truth for filter state,

predictable defaults,

and visible filter chips / indicators for anything that materially narrows the queue.

5. Add permanent guardrails

Add tests that make this class of bug impossible to reintroduce:

Backend tests

mineOnly requested + mapped rep → filtered rows return correctly

mineOnly requested + no mapped rep → endpoint returns structured warning / safe fallback, never silent empty lie

list/snapshot consistency for org rows vs user-scope rows

Frontend tests

generic empty state is not shown when warningCode = NO_QUOTE_REP_MAPPING

honest empty state renders with “Show all” CTA

clicking “Show all” clears Mine only and loads rows

saved-view restored mineOnly is visible and understandable in the UI

Product-level guardrail

Add an assertion or integration test for this contract:

If org-level Quote Requests count for the current window is > 0, and list returns 0 because of user-scope/filter state, the UI must explain why. It must never imply “there is no work” without naming the narrowing condition.

6. Acceptance proof I want back

Show me all of this with production-backed evidence:

The exact root cause

files/functions involved

whether Mine Only, saved view restoration, or both were involved

Before/after behavior for the exact failing user/session

before: misleading generic empty state

after: honest state or real rows

Three screenshots / proofs:

Mine Only on + unmapped user → honest warning state

Show all clicked → rows visible

Mapped rep user with Mine Only on → proper personal queue behavior

Tests added

list them explicitly

explain which ones prevent this regression permanently

Hard constraints

Do not touch the capture-first contract, classifier, routing_status logic, or backfill unless you prove they are involved in this exact UI trust bug.

Do not redesign the whole Customer Quotes page.

Fix the trust bug at the root: user scope, filter truth, saved-view truth, and honest empty states.

Standard

I am done accepting states where the app technically has data but presents “0” or “No matches” with no truthful explanation.

The permanent contract is:

if there is no work, say there is no work

if filters hide the work, say which filters

if user mapping prevents a personal queue, say that explicitly

if the user can fix it, give them the button to fix it immediately

I want the smallest complete fix that permanently restores trust in Quote Requests.